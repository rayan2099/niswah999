# 00_12 — Traceability Matrix

**Generated**: Niswah Full Implementation Reconciliation wave, 2026-09-14. Discovery only — no remediation performed.

Each row: Requirement → Implementation → Persistence → Tests → Runtime evidence → Production evidence, classified per the charter's own scheme: **VERIFIED**, **PARTIAL**, **MISSING**, **REGRESSED**, **UNTESTED**, **LIVE_VERIFICATION_REQUIRED**, **PRODUCTION_DRIFT**, **DEFERRED**.

| Requirement | Implementation | Persistence | Tests | Runtime evidence | Production evidence | Classification |
|---|---|---|---|---|---|---|
| `REQ-FIQH-001` Madhhab "I don't know" path (`AUTH-010`) | Onboarding now offers a real 5th choice + explanation + suggestion-confirmation flow; Settings mirrors it; `MadhhabSuggestionService` now genuinely wired in; **confirmed still genuinely present at `terminal` HEAD, 2026-09-15, via direct `git show` + a full re-run of the Madhhab test suite against that exact commit** | `MadhhabSelectionState` enum (`unset`/`unknown`/`selected`) added, distinct from `Madhhab` | 39/39 onboarding widget tests passing (EN+AR, full sub-flow); 10/10 `madhhab_controller_test.dart` | **Owner E4 retest FAILED, 2026-09-15**: live screenshot showed only the original 4 choices, no "I don't know my Madhhab" option — **reclassified `STALE_BUILD_EVIDENCE` / `INVALID_FOR_CURRENT_RELEASE`, Release Branch Reconciliation wave, 2026-09-15 (same day)**, once the source-branch mismatch was proven, not merely hypothesized; historical record preserved verbatim, not erased | Root-caused, 2026-09-15: `main` (GitHub's default branch) had been frozen 97 commits behind `terminal` since before this engagement began, zero PRs ever merged — the owner's tested build was compiled from stale/`main`-equivalent source, not from `terminal`. `main`'s own `onboarding_screen.dart` confirmed (via `git show main:...`) to contain only the old 4-choice grid. This is a deployment/branch gap, not a code regression. **Update, same day**: `main` was independently found to have since advanced to a later (though still pre-fix) commit, and PR #1 (`terminal` → `main`) is open to close the gap entirely — see `00_09`'s §82 | **REMEDIATED — `E2_AUTOMATED_VERIFIED`** (reverted from the same-day `REOPENED`/`E4_FAIL` status once that status's own premise — that the tested build reflected current `terminal` — was disproven; the underlying code fix is intact and re-verified on `terminal` HEAD; closure requires a new owner retest against a build whose commit SHA is confirmed contained in the reconciled release branch; not a whole-app blocker) |
| `REQ-FIQH-002` Madhhab server persistence (**= `AUTH-005`, already-tracked, corrected mapping**) | `MadhhabController` rewritten `LOCAL_CACHE_OF_SERVER`; signup trigger's hardcoded `'HANBALI'` removed | `users.madhhab` (nullable) + new `users.madhhab_selection_state`, both constrained; a conflicting undocumented legacy constraint found and dropped | `madhhab_controller_test.dart` (10/10); `cycle_status_engine_test.dart`/`fiqh_report_insights_engine_test.dart` UNKNOWN/UNSET cases | A real lowercase write proven live inside a rolled-back transaction | Migration `20260914120000_...` + follow-up `20260914120100_...` applied and read-back-verified live | **REMEDIATED — `E3_SERVER_VERIFIED`** (`AUTH-005`, Fiqh Remediation Wave 1, 2026-09-14; owner E4 retest required for closure; not a whole-app blocker) |
| `REQ-FIQH-010`/`016` Structured fiqh KB / retrieval / source-backed AI context (`FIQH-8`, new) | Does not exist — `fiqh-advisor-chat` uses Gemini model knowledge + live Google Search grounding + a 2-domain citation allowlist, no DB-backed corpus | No KB table anywhere in live schema or migrations (confirmed via exhaustive grep) | None (nothing built to test) | Confirmed via direct edge-function source read + schema grep this pass | Gap live in production today | **MISSING — HIGH FIQH-FEATURE LAUNCH BLOCKER** if described as scholar-approved-KB-backed; not blocking with honest framing |
| `REQ-FIQH-017` Scholar review status (`FIQH-9`, new) | `SCHOLAR_REVIEW_PACKAGE.md` drafted, reviewer-ready | n/a | n/a | 0 of 16 sources / 12 golden cases / 13 geo-mappings / 7 jurisdictions reviewed — confirmed via direct file inspection | No evidence package has reached an actual scholar | **MISSING — HIGH FIQH-FEATURE LAUNCH BLOCKER** if fiqh guidance is claimed scholar-approved; not blocking with honest framing |
| `REQ-ONBOARD-001` No auth UI once signed in (`AUTH-008`) | `main.dart` root guard; no embedded login step in onboarding | `users.onboarding_completed` | `auth_onboarding_routing_test.dart`, `onboarding_ui_test.dart` — all passing | **Owner fresh-install iOS E4 retest PASS, 2026-09-14** — no second Sign In/Sign Up, Sign Out→Sign In→dashboard all confirmed | Fix present in current commit, now confirmed running on owner's real device | **VERIFIED_CLOSED / E4** |
| `REQ-ONBOARD-002` No redundant language step (`AUTH-009`) | Language step removed from onboarding; `AppLocaleController` sole authority | `niswah_arabic` (local, by design) | New root-router + onboarding tests, all passing | **Owner fresh-install iOS E4 retest PASS, 2026-09-14** — language selected pre-auth, no duplicate screen | Fix present in current commit, now confirmed running on owner's real device | **VERIFIED_CLOSED / E4** |
| `AUTH-007` Arabic locale/RTL | `_arabic` getter, single-source locale | `niswah_arabic` | Full AUTH-007 suite | **Owner fresh-install iOS E4 retest PASS, 2026-09-14** — Arabic onboarding remained Arabic | Fix live in current commit, now confirmed running on owner's real device | **VERIFIED_CLOSED / E4** |
| `REQ-ONBOARD-003` Marital status server persistence | `MaritalStatusController` | Local only, no Supabase column found | None (nothing to test) | Confirmed via code grep this pass | Gap live | **MISSING**, classified `LOCAL_ONLY_UNSAFE` (not launch-blocking) |
| `REQ-ARCH-001` No duplicate state-authority stacks | 3 confirmed live/dead pairs (Settings, cycle logging, prayer tracking) | Dead stacks target `cycle_logs`/`profiles.selected_madhhab` (live tables, unused) | None on the dead stacks (expected) | Confirmed via exhaustive grep this pass — zero external references to any dead stack | Dead code ships in bundle, never executes | **REGRESSED-RISK** (not currently live-divergent; real risk on future edit) |
| `REQ-ONBOARD-004` "Journeys" feature reachable (`PJ-007`, new) | `GuidedJourneysScreen` fully built, zero navigation references | Unknown (not inspected) | Not checked (unreachable) | Confirmed unreachable via grep this pass | Ships, unreachable | **MISSING** (wiring only, not launch-blocking) |
| `REQ-ONBOARD-005` "Privacy" step matches its name | Step 2/9 = Anonymous Mode toggle only; real consent capture is on the sign-in step | `profiles.anonymous_mode` (correct, single authority) | `sign_in_consent_gating_test.dart` covers the real consent gate | n/a | Current, live | **PARTIAL** (non-blocking but recommended) |
| `REQ-DATA-001` Full data export coverage | `data_export_builder.dart`, 8 sections | Missing 7 confirmed-live tables (see ledger) | Not independently confirmed this pass | Confirmed via direct code-vs-schema comparison this pass | Gap live | **PARTIAL** (existing `PC-006`; non-blocking but required before "full export" can be honestly claimed) |
| `REQ-DATA-002` Account-deletion local cleanup coverage | `local_sensitive_data_cleanup.dart`, 2 categories | Missing ~13 confirmed local keys (see ledger) | Not checked | Confirmed via direct comparison this pass | Gap live | **PARTIAL** (existing `PC-007` addendum; non-blocking but recommended) |
| `REQ-NOTIF-001` Notification silent-failure current state | `notification_service.dart` | n/a (100% local) | Traced in `PJ_journey_traces.md`; current fix state not re-confirmed this pass | Not re-tested this pass | Unknown | **UNTESTED** (not launch-blocking; future Notifications Reliability wave) |
| `REQ-CYCLE-001` Wellbeing local-cache divergence risk | `dashboard_screen.dart` local cache vs `WellbeingRepository`/`wellbeing_logs` | Dual (local UI cache + server record) | Not checked | Not tested | Current, live | **UNTESTED** (not launch-blocking; future Wellbeing/data-integrity wave) |
| `AUTH-001` (referenced, not re-litigated here) | `site_url`/`uri_allow_list`/all 13 email templates applied and read-back verified | Supabase Auth project config | Automated pre/post-apply checklist, full regression | **Owner E4 PASS** on 5/6 components (SMTP, sender identity, templates, token processing, server-confirmed state); confirmation-destination/fallback still open | **Confirmed live this pass** via a fresh, direct Management API read (site_url, allow_list, SMTP host, all template custom-flags true) | **LIVE_VERIFICATION_REQUIRED** (narrowed — see Production Drift Report) |
| ~35 other findings (see ledger's "already tracked" section) | — | — | Spot-checked, code/tests confirmed present and unregressed | Mixed (per finding) | Mixed (per finding) | **VERIFIED** per finding register, re-confirmed this pass |
| `UI-001` Global loading-indicator visual defect (button spinners collapsing to a dot) | `NiswahLoadingIndicator`/`NiswahLoadingButton` (`lib/core/widgets/niswah_loading_indicator.dart`), applied at 7 sites across 4 files | n/a (client-side rendering only) | `niswah_loading_indicator_test.dart`, 9/9 passing (size, no-overflow at 200% scale both languages, small viewport, stable button size, double-submit protection) | **Owner E4 PASS, 2026-09-15**, commit `211ee76d535f3090cf6c250c5d6faeea672d2e2a`: Create Account spinner clearly visible and animated, button dimensions stable, one additional loading state (profile save) also confirmed | Live-verified on the owner's own device | **VERIFIED_CLOSED (E4)** — historical root-cause mechanism never conclusively reproduced against the audited source (disclosed, not glossed over); ~15 already-correct, not-yet-migrated sites tracked as non-blocking cleanup |
| `REQ-LOC-001` Onboarding "Use Current Location"/city-selection visible confirmation (`AUTH-011`, new) | `_LocationStatus` state machine (idle/detecting/selected/error) + always-visible `_LocationStatusBanner` in `onboarding_screen.dart`; reuses `NiswahLoadingIndicator` for the in-flight spinner | Persistence itself unchanged (`PrayerLocationController.select`/`useDeviceLocation`, already correct); this fix is UI-confirmation-only | New `GeolocatorPlatform`-mocked test group, 8/8 passing (idle/detecting/selected/error states, both device-location and city-preset paths, EN+AR, Skip non-interference, busy-lockout) | Not tested on a real device/GPS this pass — no physical hardware available | Not yet deployed to any pipeline the owner has tested (see the branch-divergence note below) | **REMEDIATED — `E2_AUTOMATED_VERIFIED`** (Live Onboarding Contradiction Investigation wave, 2026-09-15; owner E4 retest required for closure, including a real successful GPS fix; not a whole-app blocker; distinct from `AUTH-006`, which tracks the separate, still-open server-persistence gap) |
| `REQ-STARTUP-001` Authenticated-session startup never resolves (`AUTH-012`, new) | `AuthController`'s `OnboardingStatus` enum (`loading`/`incomplete`/`complete`/`error`) replaces the ambiguous `bool?` contract; request-generation guard; 15s timeout; `main.dart`'s router switches explicitly on all 4 states with a new `_AuthGateErrorScreen` (Retry + Sign out) | No persistence change — this is a client-side routing/state-model fix; live production data independently confirmed healthy (0 orphaned `auth.users`/`public.users` rows among 24 real accounts, `onboarding_completed` genuinely `NOT NULL DEFAULT false` with 0 NULLs, RLS `users_read_own` correctly permits own-row reads) | New `test/auth_gate_test.dart`, 12/12 passing (server incomplete/complete, thrown exception, missing row, slow response, 15s timeout, retry-after-failure, logout-in-flight, account-switch-in-flight, router renders error screen EN+AR, router renders loading not error) | **Owner reproduced live** on a clean rebuild from reconciled `main` (`27e53f50bd8145babba6e129e4abf3e31522944a`, all CI green): white screen → correctly-rendering spinner (not `UI-001`) → spins indefinitely, never reaching Sign In/Onboarding/Dashboard | Root-caused via direct code trace (not guessed): a `null`-vs-`null` equality guard silently suppressed `notifyListeners()` whenever the onboarding-status check failed for any reason, and the root router had no error/timeout/retry state to fall into regardless | **REMEDIATED — `E2_AUTOMATED_VERIFIED`** (Startup Auth-Gate Infinite-Spinner Investigation, 2026-09-15; owner E4 retest required for closure — no live simulator/device access available to this session to directly reproduce the owner's own exact stuck session before or after the fix; **CRITICAL WHOLE-APP LAUNCH BLOCKER**, unlike `AUTH-005`/`AUTH-010`/`AUTH-011`'s Fiqh-feature/onboarding-only scope, since it can strand any authenticated returning user regardless of which features are enabled) |
| `REQ-FIQH-020` Haidh-logging entry gate on unresolved Madhhab (`AUTH-010`, reconciled — not new) | One canonical gate inside `showCycleLogSheet` (all 6 "Log my Haidh" call sites already funnel through it), opening `MadhhabResolutionScreen` (new, `lib/features/madhhab_resolution/`) whenever `mode == period` and `!MadhhabController.instance.isSelected`; `symptomsOnly` mode (raw wellbeing logging) deliberately left ungated | Persistence unchanged — calls the existing, already-correct `MadhhabController.selectMadhhab`/`selectUnknown` | New `test/madhhab_resolution_test.dart`, 19/19 passing (gate for SELECTED/UNSET/UNKNOWN, symptomsOnly never gated, direct + guided selection, Settings reuse, account isolation, EN/AR, 200% scale, searchable picker) | Owner reported the shipped assistance flow (free-text residence-country question) too weak, frequently returning "no suggestion available" with no further path | Traced to the original flow's single-signal, free-text design — not a regression, a genuine robustness gap in the originally-shipped assistance behavior | **REMEDIATED — `E2_AUTOMATED_VERIFIED`** (Madhhab Resolution Gate wave, 2026-09-16; reconciled into `AUTH-010`, not a new finding; owner E4 retest required for closure; not a whole-app blocker) |
| `REQ-FIQH-021` Guided Madhhab signal hierarchy — no geographic auto-classification (new, part of `AUTH-010`'s reconciliation) | Upbringing question outranks family/community question, both via a searchable ~195-country ISO-coded picker; current residence never consulted at all; existing `MadhhabSuggestionService` dataset (10 countries/1 region, medium-confidence only) reused unexpanded, now carrying `mapping_id`/`evidence_source_ids`/`reviewer_status`/a dataset version | n/a (suggestion-only; no persistence until explicit confirmation) | Same suite as above — explicit Canada (unsupported, upbringing+family both insufficient) and Egypt/Saudi Arabia (supported, single/multi-candidate) cases, plus a conflicting-signals case proving the hierarchy order | Charter explicitly prohibited a country→one-madhhab model and residence-based auto-classification | Confirmed via direct dataset inspection — the existing seed mapping was never expanded or invented from model knowledge, per explicit instruction | **VERIFIED** — residence is structurally never fed into the resolution logic at all (not merely down-weighted), so it is trivially, provably never sufficient alone |

## Menstrual Data Integrity & Active Bleeding Journey — Hardening 5 / Commits D-H (2026-09-18/19, updated 2026-09-19 with F7/F8/notification-continuity, updated 2026-09-20 with 7 independent-review fixes, PR #4, `feat/menstrual-data-integrity`, unmerged)

Full architecture, per-item evidence, and every code/test reference live
in `docs/menstrual-data-integrity-contract.md` (the authoritative,
continuously-updated source for this charter) — this table is a
summary cross-reference, not a duplicate.

| Requirement | Implementation | Persistence | Tests | Runtime evidence | Classification |
|---|---|---|---|---|---|
| Hardening 5 — one canonical mutation boundary (no direct client DML on raw evidence tables) | `bleeding_episodes`/`bleeding_observations`/`cycle_baselines` grants revoked; 8 `SECURITY DEFINER` RPCs are the sole write path | Schema migration `20260917090000` + RPC migrations `20260917100000`–`20260920090000` | Live adversarial matrix (direct INSERT/UPDATE rejected, `anon` rejected outright including RPC execution, cross-account rejected) | **E3** — verified live against a full local Postgres reconstruction this pass, not merely code-reviewed | **REMEDIATED — E3_SERVER_VERIFIED** (owner E4 retest not performed) |
| Commit D1/D4 — daily check-in + backfill | `daily_checkin_sheet.dart`, `record_bleeding_observation` RPC | `bleeding_observations` | `correction_sheet_test.dart`-style honest-failure widget tests; live idempotent-replay + same-day-3x proofs | **E3** for the RPC; **E2** for the UI (no live session in a widget test, matching this codebase's own established testing discipline) | **REMEDIATED — E2/E3 mixed**, owner E4 retest not performed |
| Commit D5/D7 — correction + concurrent-conflict resolution | `correction_sheet.dart`, `correct_observation` RPC's `ERRCODE='NW409'` tip-check | `bleeding_observations.supersedes_id` | Live conflict-trigger proof; widget tests for the form/conflict UI's no-session path | **E3**/**E2** | **REMEDIATED — E2/E3 mixed** |
| Commit D9 process-death replay — reconciliation return-value defect | `reconcilePendingOperations` did not check `endEpisode`/`recordObservation`/`correctObservation`/`saveBaseline`'s nullable return before clearing a pending operation, silently treating a genuine RPC failure as success | `PendingBleedingOperationStore` (`SecureLocalStore`) | New unit tests for every affected operation type | **E2** — found and fixed via code reasoning this pass, confirmed via the existing "leaves pending" test pattern | **REMEDIATED — E2_AUTOMATED_VERIFIED** (a genuine defect predating this specific wave for the `endEpisode` case) |
| Commit E — ACTIVE_BLEEDING_CHECKIN notification (eligibility/consent/privacy/timezone/identity/satisfaction/tap-routing/audit/end-cancel) | `ActiveBleedingReminderScheduler`, `NotificationService` payload+tap-capture, `NotificationRefreshCoordinator._refreshActiveBleeding` | `NotificationPreference` (SharedPreferences), `NotificationLogController` | 15 new unit tests (`notification_scheduler_test.dart`) covering eligibility/identity/satisfaction/copy/payload | **E2** — pure-logic unit tests only; real OS notification delivery and tap-through were never exercised on any device | **PARTIAL — E2_AUTOMATED_VERIFIED**, owner E4 retest required for closure (real device tap-routing, real permission grant/deny, real DST transition) |
| Commit F1/F2/F3/F4/F5 — canonical ring states eliminating the `cycle_entries`-projection dependency | `CanonicalBleedingStatusResolver`, wired into `dashboard_screen.dart`'s `isCurrentlyBleeding` union and card selection | Reads `bleeding_episodes` directly | `canonical_bleeding_status_resolver_test.dart`, 6/6 | **E2** — the resolver is proven structurally incapable of depending on the projection (no code path connects them); no separate dashboard-widget-level integration test forcing a live projection failure was built | **REMEDIATED — E2_AUTOMATED_VERIFIED**, owner E4 retest required for closure |
| Commit F7 — observed-vs-predicted-vs-estimate-vs-legacy-unverified-vs-uncertain visual/semantic provenance, non-color-only, **revised 2026-09-20 (Independent Review Fixes wave, Fixes 2 and 5)** | `EvidenceProvenance` (6-class taxonomy), `ProvenanceBadge` (icon+text, Semantics long-form label), wired into the new canonical calendar; a genuine pre-existing screen-reader bug fixed along the way (`cycle_calendar.dart`'s day-cell Semantics label always announced English regardless of locale). **Fix 2**: the calendar's own uncertainty announcement no longer collapses "no report" and "explicit uncertain report" into the same phrase — four distinct states now. **Fix 5**: an audit against real production data sources found 2 of the 6 categories (`legacyUnverified`, `userReportedEstimate`) had no code path ever attaching them to a real record — a legend-only claim. Both now have a genuine surface: `legacyUnverified` via the legacy `CycleCalendar` widget (the one place such a record can exist), `userReportedEstimate` via a new labeled summary reading the previously-uncalled `CycleBaselineRepositoryImpl.getBaseline`, never attached to a specific day | n/a (read-only presentation layer) | `provenance_badge_test.dart` (16), `canonical_calendar_screen_test.dart`'s Fix 2/4/5 groups, new `cycle_calendar_legacy_provenance_test.dart` (3) — 27 total across all F7-related files | **E2** — pure widget-level automated coverage; not tried on a real device/screen reader | **REMEDIATED — E2_AUTOMATED_VERIFIED** (2026-09-20), owner E4 retest required for closure |
| Commit F8 — canonical calendar (month/year navigation, tap-to-inspect, multi-observation days, revision history, backfill, correction, bounded/disclosed prediction, refresh-on-return), **revised 2026-09-20 (Independent Review Fixes wave, Fixes 3 and 4)** | New `CanonicalCalendarScreen` + `observation_detail_sheet.dart`, reading `bleeding_episodes`/effective `bleeding_observations` directly (never `cycle_entries`); reuses the existing, already-hardened `showCorrectObservationSheet`/`showBackfillObservationSheet` write paths rather than a second one; wired into the dashboard via a new calendar icon. **Fix 3**: the "corrected" indicator was computed as same-day observation count (`> 1`) — three genuinely independent same-day reports were wrongly flagged as corrected. Now computed via `supersedes_id`, the real revision-chain fact. **Fix 4**: `LoadDegraded` (partial, quarantined-row) reads were silently folded into clean success — no notice, and a prediction could be built from a data set known to have a gap. Now tracked separately, shown via a distinct retryable notice, and the forward prediction is suppressed entirely whenever either read is degraded | Reads `bleeding_episodes`/`bleeding_observations` directly; writes go through the existing Commit D RPCs, unchanged | `canonical_calendar_screen_test.dart` (27 total — the original 10 plus Fix 2 (5), Fix 3 (4), Fix 4 (5), Fix 5 (3)) | **E2** — a full write round trip (save → server → refreshed calendar) requires a real Supabase session, the same established boundary `correction_sheet_test.dart` already documents; not tried on a real device | **REMEDIATED — E2_AUTOMATED_VERIFIED** (2026-09-20), owner E4 retest required for closure. Disclosed scope limit, not silently narrowed: adding a fully separate historical episode (start AND end both in the past, unconnected to today) has no write path anywhere in this app outside onboarding — "add missing entry" covers backfilling within an *existing* episode's own range only, consistent with this codebase's own established Commit D4 "backfill" scope; a day outside any episode range points to "Start Bleeding" instead of a fabricated action |
| Canonical Fiqh regression fix — the manual-Istihadah test broken by Commit G4's own legacy-fallback removal, plus a real, independently-found safety gap | Fixed the test properly (genuine canonical-evidence fixtures, not a restored legacy fallback); found and fixed that the "insufficient evidence while factually bleeding" guard checked an enum value (`FiqhCycleState.insufficientHistory`) the fallback snapshot never actually produces (it's hardcoded to `.tahara`) — the guard never fired, so `_PrayerStatusCard` could show "Salah is obligatory" for a woman factually bleeding whose observations simply hadn't synced yet; now checks `!fiqhCalculation.hasSufficientHistory` directly | n/a (client-side conclusion logic only) | `parity_today_stepper_consistency_test.dart` — 2 new tests (unavailable evidence → explicit unresolved state; genuinely insufficient evidence while factually open → never confident Tahara) plus the pre-existing manual-Istihadah test repaired; zero newly-failing tests versus the `915af02` baseline (confirmed via a worktree comparison) | **E2** — pure widget-level automated coverage | **REMEDIATED — E2_AUTOMATED_VERIFIED** (2026-09-19) |
| Notification continuity beyond 7 days — design review + implementation, **revised 2026-09-20 (Independent Review Fixes wave, Fix 1)** | ~~Two-tier design: 7-day rolling window + OS-native recurring fallback~~ **superseded — the two-tier hybrid was found, via direct inspection of the installed plugin's native source, to be platform-inconsistent: iOS's time-only calendar trigger discards the requested date and fires at the next matching clock time regardless, meaning the "delayed fallback" would have fired starting day 1, genuinely duplicating every rolling-window day on iOS.** Single mechanism now: the same one-off, per-day-exact `zonedSchedule` call, widened from 7 to **30 days** (`ActiveBleedingReminderScheduler.defaultRollingWindowDays`) — no recurring layer, no possibility of overlap by construction. Also fixed a real, independently-found gap — ending an episode previously cancelled only *today's* rolling-window id, leaving the rest of an already-scheduled window live for an episode that no longer exists; the existing cancellation loop (`rollingWindowReminderIds`) now covers the full, larger window automatically | `NotificationPreference` (unchanged); no new persistence | `notification_scheduler_test.dart` (window-id enumeration, 15-day full-coverage proof, day-30 boundary, DST-spanning id stability, iOS budget audit — 34/64 worst-case with the new 30-day size), `notification_multi_day_continuity_test.dart` (proves exactly one `zonedSchedule` call per id across 15+ days, and zero `matchDateTimeComponents`-based registrations for this reminder type — i.e., proves no duplicate is possible), `dashboard_notification_tap_stream_test.dart` (5 — the live `onTap` subscription while the dashboard is already mounted) | **E2** — real mocked-plugin-channel + unit coverage, plus direct native-source verification (not assumed) of the platform behavior that motivated this redesign; real device delivery, real timezone/DST change, and real permission grant/deny were never exercised | **REMEDIATED — E2_AUTOMATED_VERIFIED** (2026-09-20). Exact honest supported horizon, stated plainly: per-day-exact reminders (skip-if-already-done, tap routes to that specific date) are guaranteed for 30 days without reopening — a single, predictable mechanism, not a bounded-precise-plus-indefinite-coarse hybrid. Beyond 30 days without ever reopening, reminders genuinely stop (the same honest limit any client-only scheduler has) — this is NOT a claim of indefinite tracking. Owner E4 retest required for closure |
| Remaining widget-test coverage (charter Section 5) | `_CanonicalStatusUnavailableCard`/`_FiqhEvidenceUnresolvedCard` after a genuine canonical-read failure or degradation, notification tap stream while the dashboard is already mounted, F8's backfill/correction/revision-history flow, F7's provenance visual/Semantics contract | n/a | `dashboard_canonical_degraded_evidence_test.dart` (5, new), `dashboard_notification_tap_stream_test.dart` (4, revised — the recurring-fallback-specific test was removed along with Fix 1's own removal of that mechanism), `canonical_calendar_screen_test.dart`/`provenance_badge_test.dart`/`cycle_calendar_legacy_provenance_test.dart` (see F7/F8 rows above); a genuine `_FiqhEvidenceUnresolvedCard` text-overflow bug (English copy ~10px too tall for its fixed circle at real device width) found and fixed along the way | **E2** | **REMEDIATED — E2_AUTOMATED_VERIFIED** (2026-09-19, widened 2026-09-20) |
| Independent Review Fixes wave, Fix 6 — regression-baseline reconciliation | Re-verified the "pre-existing failures" claim against the actual PR merge base (`e4cc02e`, via a fresh `git worktree`), not merely a later PR checkpoint (`915af02`) — all 10 were confirmed genuinely present at the true merge base. The one test that did not reproduce there, `notification_multi_day_continuity_test.dart`, was introduced within this PR and had a real flakiness (an independent second real-clock read racing `BleedingEpisodeRepositoryImpl.localToday`'s own internal one) — fixed in the test itself, not by making `localToday` `AppClock`-injectable (that would violate its own deliberate, already-tested real-wall-clock-only design, PR #4 hardening Blocker 7). `NotificationRefreshCoordinator.refresh()`'s own `now` also switched from raw `DateTime.now()` to `AppClock.now()` for general consistency (harmless; no other test calls `refresh()` directly) | n/a | The fixed test itself, re-run clean across 5 consecutive executions | **E2** | **REMEDIATED — E2_AUTOMATED_VERIFIED** (2026-09-20). Final count: 739 tests, 10 failures, all 10 confirmed identical to the true merge-base baseline — zero PR-introduced regressions remain |
| Commit G1/G4/G5 — Fiqh boundary (Madhhab never gates raw tracking; Fiqh never mutates evidence; insufficient evidence resolves unresolved) | Verified via direct grep of every new write path + the existing Fiqh/calculation engines | n/a | n/a | **E2** — confirmed via static inspection this pass (zero Madhhab references in any tracking RPC/write path; zero mutation calls in any Fiqh engine file) | **VERIFIED** — already true by construction, not newly built |
| Commit G2/G3 — Fiqh assessment provenance/invalidation | N/A — no persisted Fiqh assessment cache exists anywhere in this codebase | n/a | n/a | Confirmed via grep this pass (no matches for any cache table/field) | **N/A, documented as a binding forward contract** for whoever introduces the first such cache |
| Commit H — adversarial matrix (60 named scenarios) | ~45-48 of 60 have real coverage (unit test or live-Postgres proof) across this session's work; the remainder (concurrent-account-switch-with-pending-ops integration, malformed-server-row, real-device DST, notification permission grant/deny, full end-to-end tap-through) are disclosed gaps, not fabricated passes | n/a | Distributed across every test file referenced above | Mixed E2/E3 per item | **PARTIAL** — see the final PR #4 report's own 20-proof-chain breakdown for the item-by-item split |
| Export/deletion audit — canonical bleeding tables | `bleeding_episodes`/`bleeding_observations`/`cycle_baselines` were missing from `data_export_builder.dart`'s `exportSections`; deletion was already correct via the existing `auth.users`→`public.users`→these tables `ON DELETE CASCADE` chain | `delete_my_account()` RPC (unchanged, cascade already covered these tables) | `data_export_resilience_test.dart`'s existing generic per-section loop now covers the 3 new sections automatically | **E2** — verified the FK cascade chain directly in the canonical baseline SQL; export gap found via direct code inspection, fixed and test-passing this pass | **REMEDIATED — E2_AUTOMATED_VERIFIED** (a genuine, previously-undetected export gap; deletion was never actually broken) |
| Privacy audit — new code this wave | Grepped every new file's `AppErrorReporter.report`/logging calls for flow/symptoms/notes/Madhhab/payload/params leakage | n/a | n/a | **E2** — zero matches found; notification payload/copy independently unit-tested to exclude sensitive terms | **VERIFIED** for this wave's new code only — not a full-repo privacy re-audit |

## Notes on classification discipline

- **VERIFIED** here means automated/code-level evidence is solid — it does **not** claim E4 live proof unless explicitly stated.
- **`AUTH-007`/`AUTH-008`/`AUTH-009` are now `VERIFIED_CLOSED / E4`**, corrected from the prior wave's `LIVE_VERIFICATION_REQUIRED`/`REOPENED` statuses, on the strength of a real owner fresh-install iOS retest reporting PASS on every covered item — the same conservative "owner evidence is authoritative" discipline that drove the `AUTH-008` reopening now drives this closure, in both directions, on the same principle.
- **`REQ-FIQH-002` was corrected this pass** from an incorrectly-implied "no finding ID" to its true canonical mapping, `AUTH-005` (already open since 2026-09-11) — a duplication error caught and fixed, not a new finding.
- Rows marked **UNTESTED** reflect genuine gaps in this pass's own coverage (time-boxed discovery, not exhaustive) — they are flagged for a dedicated follow-up check, not asserted as either broken or fine.
- **`main`/`terminal` branch divergence, discovered 2026-09-15 (Live Onboarding Contradiction Investigation wave) — the single most consequential finding of this entire engagement to date**: GitHub's own default branch, `main` (confirmed via `gh repo view --json defaultBranchRef`), has been frozen at commit `ebd201256f7b85741457bd44be1df8f26804b7fa` since before any remediation work in this engagement began. All work — every wave referenced in this document, 97 commits' worth — has been committed only to `terminal`. `git merge-base --is-ancestor <any remediation commit> main` returns NO for every commit checked; `gh pr list --state all` returns empty; zero of the 97 commits have ever been merged into `main` by any means. This means: (1) GitHub's own CI (`.github/workflows/ci.yml`, triggers only on `branches: [main]`) has never automatically run against any of this engagement's work; (2) any build/install pipeline that sources from `main` (or an equivalent stale checkout) would show **every defect this engagement has ever "fixed,"** not only the Madhhab regression (`AUTH-010`) that this specific investigation traced to it. This was root-caused, not assumed — `main`'s own `onboarding_screen.dart` was read directly (`git show main:...`) and confirmed to contain the exact old 4-choice Madhhab grid the owner's screenshot showed. No merge or branch action was taken unilaterally; this is reported for an explicit owner decision on how and when to bring `main` current, since merging shared branch state is a hard-to-reverse action outside this engagement's authority to decide alone.
- **Fiqh Authority / Knowledge-Base Governance Correction, 2026-09-14 (same-day follow-on)**: `REQ-FIQH-001`/`AUTH-010` and `REQ-FIQH-002`/`AUTH-005` are both reclassified from "not launch-blocking"/"recommendation only" to formal Fiqh-feature launch blockers (HIGH and CRITICAL respectively) — neither is a whole-app blocker; both apply only if Fiqh-guidance surfaces ship enabled. Two new findings (`FIQH-8`: no structured, database-backed fiqh knowledge base exists anywhere in the codebase or live schema; `FIQH-9`: the Scholar Review Gate is at 0% completion) were registered after direct inspection of the `fiqh-advisor-chat` edge function's full source/system prompt and an exhaustive schema/migration grep — this closes the gap left by the prior wave's discovery agents, which had not yet read that function's actual prompt text or specifically searched for a KB-shaped table. See `00_11_REQUIREMENTS_LEDGER.md`'s "Fiqh Authority / Knowledge-Base Governance Correction" section for the full two-layer (core-app / Fiqh-feature) blocker model this introduces.
- **PR #4 "Finish the Product Contract" wave, 2026-09-19**: F7 and F8 (previously **MISSING**, disclosed as such) are now built and tested — see their own rows above (since revised, 2026-09-20 — see the next bullet). The manual-Istihadah Fiqh test regression from the prior same-day wave is resolved without restoring any legacy fallback, and a real, independently-found safety gap (an open episode with unsynced evidence could show a confident "Salah is obligatory") is closed alongside it. None of this changes PR #4's own status: still **DRAFT, unmerged**; `E4` (owner device retest) still not performed for any item in this section; the Fiqh-engine/Knowledge-Base findings above (`AUTH-005`/`AUTH-010`/`FIQH-8`/`FIQH-9`) are entirely separate from this charter and are unaffected by it.
- **PR #4 "Independent Review Fixes" wave, 2026-09-20**: an independent review of the prior wave's own work found seven real issues, all addressed without restoring any legacy fallback. Most consequential: the notification-continuity design from the prior wave (a two-tier hybrid) was found to be genuinely platform-inconsistent — verified directly against the installed plugin's native source, iOS's time-only recurring trigger discards the requested date and would have fired the "delayed" fallback starting day 1, duplicating the rolling window's own reminders. Replaced with a single, predictable 30-day exact window (see its own row above). The prior wave's own regression-baseline comparison is also corrected: re-verified against the true PR merge base (`e4cc02e`), not merely a later checkpoint on the same branch — all 10 previously-reported "pre-existing" failures are confirmed genuinely present there, and the one test that was NOT (a flaky test this same PR itself introduced) is now fixed outright rather than dismissed. Final count: 739 tests, 10 failures, zero PR-introduced regressions. See `docs/menstrual-data-integrity-contract.md`'s own latest section (§11) for the full narrative and `docs/owner-e4-menstrual-journey.md` for the revised (Fix 7) portable live-device retest script.
