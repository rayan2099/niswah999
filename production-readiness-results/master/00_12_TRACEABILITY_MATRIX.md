# 00_12 — Traceability Matrix

**Generated**: Niswah Full Implementation Reconciliation wave, 2026-09-14. Discovery only — no remediation performed.

Each row: Requirement → Implementation → Persistence → Tests → Runtime evidence → Production evidence, classified per the charter's own scheme: **VERIFIED**, **PARTIAL**, **MISSING**, **REGRESSED**, **UNTESTED**, **LIVE_VERIFICATION_REQUIRED**, **PRODUCTION_DRIFT**, **DEFERRED**.

| Requirement | Implementation | Persistence | Tests | Runtime evidence | Production evidence | Classification |
|---|---|---|---|---|---|---|
| `REQ-FIQH-001` Madhhab "I don't know" path | Not present in onboarding UI; `MadhhabSuggestionService` built, unwired, unreferenced | `Madhhab` enum has no unsure value | 11/11 unit tests for the unwired service only; zero UI tests (nothing to test) | None — path does not exist | Dead code ships, never executes | **MISSING** |
| `REQ-FIQH-002` Madhhab server persistence | None | `users.madhhab` column exists, never read/written by app; `profiles.selected_madhhab` exists, written only by dead code | None | Confirmed via live schema query this pass | Gap live in production today | **MISSING** |
| `REQ-ONBOARD-001` No auth UI once signed in (`AUTH-008`) | `main.dart` root guard; no embedded login step in onboarding | `users.onboarding_completed` | `auth_onboarding_routing_test.dart`, `onboarding_ui_test.dart` — all passing | Owner E4 retest **FAILED**; this session's re-investigation found no current reproducing code path; live device re-check blocked by a proven, documented emulator/host infrastructure fault | Fix present in current commit; owner's tested build unconfirmed | **LIVE_VERIFICATION_REQUIRED** (reopened per owner authority — see `AUTH-008`) |
| `REQ-ONBOARD-002` No redundant language step (`AUTH-009`) | Language step removed from onboarding; `AppLocaleController` sole authority | `niswah_arabic` (local, by design) | New root-router + onboarding tests, all passing | Not yet retested live by owner | Fix present in current commit | **VERIFIED** (code/test) pending **LIVE_VERIFICATION_REQUIRED** (owner retest) |
| `REQ-ONBOARD-003` Marital status server persistence | `MaritalStatusController` | Local only, no Supabase column found | None (nothing to test) | Confirmed via code grep this pass | Gap live | **MISSING** |
| `REQ-ARCH-001` No duplicate state-authority stacks | 3 confirmed live/dead pairs (Settings, cycle logging, prayer tracking) | Dead stacks target `cycle_logs`/`profiles.selected_madhhab` (live tables, unused) | None on the dead stacks (expected) | Confirmed via exhaustive grep this pass — zero external references to any dead stack | Dead code ships in bundle, never executes | **REGRESSED-RISK** (not currently live-divergent; real risk on future edit) |
| `REQ-ONBOARD-004` "Journeys" feature reachable | `GuidedJourneysScreen` fully built, zero navigation references | Unknown (not inspected) | Not checked (unreachable) | Confirmed unreachable via grep this pass | Ships, unreachable | **MISSING** (wiring only) |
| `REQ-ONBOARD-005` "Privacy" step matches its name | Step 9/7 = Anonymous Mode toggle only; real consent capture is on the sign-in step | `profiles.anonymous_mode` (correct, single authority) | `sign_in_consent_gating_test.dart` covers the real consent gate | n/a | Current, live | **PARTIAL** |
| `REQ-DATA-001` Full data export coverage | `data_export_builder.dart`, 8 sections | Missing 7 confirmed-live tables (see ledger) | Not independently confirmed this pass | Confirmed via direct code-vs-schema comparison this pass | Gap live | **PARTIAL** (existing `PC-006`, newly precise) |
| `REQ-DATA-002` Account-deletion local cleanup coverage | `local_sensitive_data_cleanup.dart`, 2 categories | Missing ~13 confirmed local keys (see ledger) | Not checked | Confirmed via direct comparison this pass | Gap live | **PARTIAL** (existing `PC-007` addendum) |
| `REQ-NOTIF-001` Notification silent-failure current state | `notification_service.dart` | n/a (100% local) | Traced in `PJ_journey_traces.md`; current fix state not re-confirmed this pass | Not re-tested this pass | Unknown | **UNTESTED** |
| `REQ-CYCLE-001` Wellbeing local-cache divergence risk | `dashboard_screen.dart` local cache vs `WellbeingRepository`/`wellbeing_logs` | Dual (local UI cache + server record) | Not checked | Not tested | Current, live | **UNTESTED** |
| `AUTH-001` (referenced, not re-litigated here) | `site_url`/`uri_allow_list`/all 13 email templates applied and read-back verified | Supabase Auth project config | Automated pre/post-apply checklist, full regression | **Owner E4 PASS** on 5/6 components (SMTP, sender identity, templates, token processing, server-confirmed state); confirmation-destination/fallback still open | **Confirmed live this pass** via a fresh, direct Management API read (site_url, allow_list, SMTP host, all template custom-flags true) | **LIVE_VERIFICATION_REQUIRED** (narrowed — see Production Drift Report) |
| `AUTH-007` (referenced) | `_arabic` getter, single-source locale | `niswah_arabic` | Full AUTH-007 suite | Not yet owner-retested at E4 | Fix live in current commit | **VERIFIED** (code/test), pending E4 |
| ~35 other findings (see ledger's "already tracked" section) | — | — | Spot-checked, code/tests confirmed present and unregressed | Mixed (per finding) | Mixed (per finding) | **VERIFIED** per finding register, re-confirmed this pass |

## Notes on classification discipline

- **VERIFIED** here means automated/code-level evidence is solid — it does **not** claim E4 live proof unless explicitly stated. Several rows are "VERIFIED (code/test), pending LIVE_VERIFICATION_REQUIRED" — this is deliberate, matching this engagement's established evidence-tier discipline (a passing test suite is E2, not E4).
- **`REQ-ONBOARD-001` is the one row this pass deliberately did NOT mark VERIFIED** despite strong automated evidence, because the owner's own live device report is treated as authoritative over automated assumption, per explicit governance instruction from the prior wave. This is the correct, conservative classification, not an oversight.
- Rows marked **UNTESTED** reflect genuine gaps in this pass's own coverage (time-boxed discovery, not exhaustive) — they are flagged for a dedicated follow-up check, not asserted as either broken or fine.
