# 00_04 — Master Finding Register

**Wave 0 update (Remediation Session, 2026-09-04):** Full detail in `00_10_WAVE0_EXECUTION_REPORT.md`. Two new findings discovered via live-schema behavioral validation, added below with fresh IDs (native audit register did not and could not find these — they required live database access):

| Finding | Audit | Native severity | Master class | Summary | Status |
|---|---|---|---|---|---|
| `W0-001` | Wave 0 (Database, live-verified) | Newly discovered | **BLOCKER — VERIFIED_CLOSED (W0-003 remediation wave, 2026-09-05)** | Live production table is named `prayer_log`; `prayer_tracking_repository_impl.dart` queried `.from('prayer_entries')`, which does not exist live. Every remote prayer-tracking call failed and silently fell back to local-only data via the existing `ROOT-005` catch-all pattern. **The table-name fix (querying `prayer_log`) was made and is confirmed still in place.** Its own closure criteria required "a live remote read/write round-trip test to confirm `prayer_log` now resolves correctly" — not run until this wave. **That test has now been run and passed** (`00_09` §21 Phase D/F), against a live-schema-derived isolated environment, only possible now that `W0-003` and the newly-discovered `W0-004` (both blocking every write, independent of the table name) are also fixed | **VERIFIED_CLOSED** |
| `W0-002` | Wave 0 (Database, live-verified) | Newly discovered | **BLOCKER — CODE_COMPLETE / LOCALLY_VERIFIED (W0-002 wave, 2026-09-06), NOT YET DEPLOYED TO PRODUCTION** | Live production table is named `pregnancy_records`; `pregnancy_tracking_repository_impl.dart` queries `.from(_tableName)` where `_tableName='pregnancy_milestones'`, which does not exist live. **Deeper root cause found this wave, not previously documented**: `PregnancyTrackingScreen` (the only consumer of this code path) is unreachable from any navigation route in the entire app — confirmed via exhaustive grep, and via its own git history (present, unwired, since the very first commit). Separately, `pregnancy_records` itself — the table this finding originally assumed was the intended backing store — is **not queried by any current Flutter code at all**; it was superseded by `pregnancy_profile` (a later, correctly-designed, actively-used table) at some point, and left live but orphaned. **Resolved**: a new `pregnancy_milestones` table (`supabase/migrations/20260907090000_pregnancy_milestones.sql`) — a direct per-user child table (FK to `users(id) ON DELETE CASCADE`), matching this schema's existing `wellbeing_logs` convention, **not** nested under the orphaned `pregnancy_records` (the app's own `PregnancyMilestone` entity has never had a pregnancy-parent-id field, and `pregnancy_profile`'s own `UNIQUE(user_id)` confirms this app has no concept of multiple concurrent/historical pregnancies needing one). `pregnancy_records` is left completely untouched — not read from, not migrated from, not dropped; its own disposition (real historical data vs. genuinely vestigial) is an explicit, separate, deferred question requiring live production read access this session doesn't have. The repository was rewritten to the real table with proper `AppErrorReporter` reporting (previously silently swallowed all remote failures) and a local-authoritative-with-sync `PregnancySyncStatus` matching `CycleLog`'s established pattern. The ViewModel's hardcoded fake LMP (`DateTime.now() - 30 weeks`, regardless of any real user input) was also replaced with real `PregnancyStatusController` state. **Fully validated against a disposable isolated local Supabase stack**: 14/14 RLS/CRUD tests (own-row CRUD, cross-user read/update/delete denial, unauthenticated denial, spoofed-user-id denial, malformed-input rejection), plus a direct account-deletion cascade test (milestone rows correctly removed via the `users` cascade, zero orphans), plus a combined fresh-restore test confirming this migration applies cleanly alongside the AI Security wave's pending migration. **Unreachable-screen question resolved this wave (Product Integration wave, 2026-09-06)**: classified **DUPLICATED BY ANOTHER CURRENT FEATURE**, not an unwired bug, on strong, multi-source evidence — (1) the dashboard's `_PregnancyOverview` already provides a materially richer, embedded pregnancy-status experience (week-by-week baby-size stages, progress percent, days-to-birth, log-birth action) than `PregnancyTrackingScreen` ever did; (2) the design reference itself (`src/components/PregnancyTracker.tsx`) is embedded inside `Today.tsx`, not a standalone page — confirming dashboard-embedded was always the intended architecture; (3) `PrayerTrackingScreen` shows the identical unreachable-since-first-commit pattern, confirming this is a consistent app-wide consolidation, not a pregnancy-specific accident; (4) the screen's "daily hydration/movement/symptom tracker" concept has no basis in the design reference and no other product evidence anywhere. **Not force-fit into navigation, per explicit instruction not to expose confirmed-abandoned UX.** File deletion was attempted and blocked by this session's own safety tooling (a destructive action); resolved instead via the charter's own offered alternative — the screen/ViewModel/calculator are kept in place, unchanged in behavior, with a prominent header comment documenting the full evidence trail so this is never mistaken for an oversight again. The underlying data layer (`PregnancyMilestone`/`PregnancyTrackingRepositoryImpl`/the `pregnancy_milestones` migration) is deliberately preserved as correct, tested, dormant infrastructure — independently valid regardless of this specific UI's fate. A real, separate bug was caught and fixed in the same pass: the Data Export screen's own user-facing disclaimer text still claimed pregnancy-tracking data was excluded, even though this wave's own export fix (§26) had already started including it — corrected. **Not `VERIFIED_CLOSED`**: the migration has not been applied to production, per this wave's explicit production-safety boundary — see `00_09` §27 for the full updated production deployment package (prepared, not executed) | **PARTIALLY_REMEDIATED — CODE_COMPLETE / LOCALLY_VERIFIED, production deployment pending separate approval** |
| `W1-001` | Gemini trust-boundary Operational Closure Checkpoint (live-verified) | Newly discovered | **MEDIUM — CODE_COMPLETE / LOCALLY_VERIFIED (AI Security wave, 2026-09-05), NOT YET DEPLOYED TO PRODUCTION** | The in-memory, per-instance sliding-window rate limiter (`_shared/rate_limit.ts`, added to close `SEC-005`/`AB-002`/`AB-008`) was deployed to production and directly load-tested: 28 rapid authenticated requests against `ai-assistant-chat` produced **zero** `429` responses, because Supabase's Edge Runtime does not guarantee warm, single-instance reuse. **Replaced with a durable, Postgres-backed atomic counter** (`ai_rate_limit_counters` table + `check_and_increment_ai_rate_limit()` `SECURITY DEFINER` RPC, `supabase/migrations/20260906090000_ai_rate_limit.sql`) — identity derived exclusively from `auth.uid()`, never a client-supplied value; direct table access revoked from `authenticated`/`anon`; atomic UPSERT prevents concurrent-request races. **Fully validated against a disposable isolated local Supabase stack** built from the current canonical baseline: all 13 required scenarios pass (first/quota/over-limit/repeated-over-limit/window-reset/separate-users/separate-functions/concurrency/unauthenticated/spoofed-identity/direct-table-access/malformed-input/cleanup), plus a real end-to-end HTTP concurrency test against the actual `ai-assistant-chat` function — **25 concurrent requests against a 15-request quota: exactly 15 allowed, 10 rejected, 0 unexpected, 0 Gemini calls among the rejected** — and confirmed the `dr-niswah-chat` red-flag exemption (`AB-008`) still bypasses the limiter correctly even when the user is far over quota. Fail-closed limiter-unavailable behavior (503, no Gemini call) also directly tested. **Not `VERIFIED_CLOSED`**: per explicit instruction, this migration has not been applied to production and the deployed architecture has not been load-tested — see `00_09` §25 for the full production deployment package, prepared but not executed | **PARTIALLY_REMEDIATED — CODE_COMPLETE / LOCALLY_VERIFIED, production deployment pending separate approval** |
| `W2-001` | Reliability + Observability Persistence-Recovery Remediation (analysis) | Newly discovered | **MEDIUM** | Evaluated per explicit instruction: does the new cycle sync-status/retry behavior itself create any additional reliability risk? One genuine, pre-existing (not newly introduced) residual risk identified: a `pending` entry has no durable recovery path beyond local device storage (`shared_preferences`) — if the app is uninstalled, local storage is cleared, or the device is lost/replaced before that entry ever successfully syncs, it is permanently lost with no way to detect or recover it, since it never reached the server. The new automatic-retry mechanism (`RR-001`'s fix) reduces how often this window is ever reached (a transient failure now gets multiple automatic retry chances instead of one terminal attempt) but does not eliminate the underlying local-only-until-synced window — that would require a fundamentally different architecture (e.g. a durable local outbox surviving reinstall, which `shared_preferences` cannot provide) | OPEN — inherent to the local-first design; mitigated, not eliminated, by this pass |
| `W0-003` | Backup/Recovery Remediation Wave, behavioral restore validation (2026-09-05) | Newly discovered | **BLOCKER — VERIFIED_CLOSED (2026-09-05)** | `prayer_tracking_repository_impl.dart` sent `PrayerStatus` enum values (`pending`/`completed`/`missed`/`excused`) that did not match the live database's `prayer_log_status_check` CHECK constraint (`prayed`/`qadha_required`/`lifted`/`missed`) — only `missed` overlapped, confirmed against the live-captured schema. **Fixed**: an explicit, bidirectional persistence mapping (`prayerStatusToDbValue`/`prayerStatusFromDbValue`) scoped to exactly the repository's remote boundary — `completed↔'prayed'`, `missed↔'missed'`, `excused↔'lifted'`; `pending` (never actually sent by the UI) throws defensively rather than silently writing an invalid value; unmapped database values (`qadha_required`) are reported via `AppErrorReporter`, not silently coerced. **Validated end-to-end** against a live-schema-derived isolated environment using the real repository code: all 3 real statuses round-trip correctly, updates work, no duplicates, the unknown-value path reports as designed, and the database's own constraint correctly rejects a truly invalid value. See `00_09` §21 for full evidence, including a real timezone bug caught and fixed in the same pass (see `W0-004`) | **VERIFIED_CLOSED** |
| `W0-004` (**new**) | W0-003 remediation, Phase D behavioral validation (2026-09-05) | Newly discovered | **BLOCKER — VERIFIED_CLOSED (2026-09-05)** | A second, distinct, currently-live production defect found while validating `W0-003`, in the exact same write path: `prayer_log.scheduled_time` is `timestamp with time zone` in the live schema, but `savePrayer()` sent `TimeOfDay.toJson()`'s `{hour, minute}` JSON object — every remote prayer_log write failed on this alone, independent of the status value, meaning `W0-003`'s fix could not have restored any real functionality without this also being fixed. **Fixed** with the same remote-boundary-only pattern; an initial version used `.toLocal()`, which made the round trip depend on the running machine's timezone — caught by its own test and corrected to a timezone-neutral UTC encoding (prayer times are a local wall-clock concept throughout this app, not a true cross-timezone instant). **Validated end-to-end** against the live-schema-derived isolated environment, confirmed on a second independent run after the timezone correction | **VERIFIED_CLOSED** |

Status changes to existing findings from Wave 0 (full evidence in `00_10`): `BR-007` resolved (Storage confirmed unused); `DI-012` upgraded from structural inference to behaviorally confirmed (account deletion cascades away the other conversation participant's own messages too); `DI-001`/`BR-002` further deepened (two new `UNKNOWN`-classified live-only objects: `chat_history`, `secret_vault`); `PC-002` scope further informed (`delete_my_account()` confirmed to work correctly and completely, but its shared-conversation cascade needs product/privacy sign-off before client-wiring).


Status: **ALL WAVES COMPLETE (1–6).** Wave 6 (Post-Launch Monitoring) plan authored at `production-readiness-results/post-launch/PL_monitoring_plan.md` — infrastructure to execute it does not yet exist (see that document's Part B). Final master consolidation is in `00_05` through `00_08`. **Final master verdict: 🔴 NO-GO** — see `00_08_FINAL_PRODUCTION_READINESS_REPORT.md`.

Status: **Wave 5 COMPLETE.** Final Pre-Launch User Journey (PJ) audit ingested below — **NO-GO**, with 4 new PJ0 (Critical) and 1 new PJ1 (High) finding discovered only through end-to-end cross-layer tracing, not visible to any single specialist audit.

**The single most consequential new discovery:** `PJ-001` traces the exact mechanism behind `DI-004`'s open critical unknown — 7+ core tables (including chat threads/messages, the `flagged_conversations` safety log, cycle entries, and community content) FK to a bare `public.users` table that no code path in the entire app ever writes to, while the codebase's own migration history is internally inconsistent about whether tables should reference `auth.users` or this bare `users` table. If live-unverified `DI-004`/`PJ-001` resolves unfavorably, this is very likely the same failure `BR-002` already proved by direct migration-replay trace, now identified as a *live, ongoing* write-path failure, not just a schema-recoverability one.

Chained onto that, `PJ-004` found that when a red-flag (urgent, safety-relevant) message's backend call fails, **three separate persistence failures stack**: the clinical audit-log entry, the user's own message, and even the reassuring safety banner shown to her are all lost — the entire exchange vanishes with zero trace on next app open. `PJ-006` found this isn't just a gap but an architectural guarantee: the Doctor's Report red-flag section cannot distinguish "nothing happened" from "every safety record failed to save," so a real doctor reading it has no way to know it's incomplete. `PJ-005` pinpointed the exact live trigger for the private-messaging demo-mode fallback (CQ-007): a per-screen-open session check that silently and repeatedly drops a user into fabricated conversations the moment her auth session dies mid-use, with no re-authentication prompt ever shown.

Status: **Wave 4 COMPLETE.** Privacy & Compliance (PC), Accessibility & UX (AU), and Analytics & Business Events (AE) ingested below.

**Wave 4 specialist verdicts:** Privacy & Compliance **NO-GO** · Accessibility & UX **NO-GO** · Analytics & Business Events **N/A** (independently re-confirmed with full evidence, not merely accepted from the provisional lean — see `AE-001`, a non-blocking observation about a dead, fully-priced "Niswah Plus" paywall UI that corroborates DI-004/DI-008's dormant premium-tier columns from the UI side).

Key new evidence: Privacy & Compliance found the sign-up consent checkbox is **purely cosmetic** — its state is never read by any sign-up button, so the app collects health/pregnancy/religious/location data from every user with no functioning consent gate at all (PC-001), and there is no account-deletion capability anywhere in the app despite the database being correctly designed to cascade it (PC-002). Accessibility & UX found 15 of 20 icon-only buttons have no accessible name for screen readers (AU-001, the audit's sole AU1), two of the app's four core fiqh-state colors fail WCAG AA contrast as text (AU-003), and — like Accessibility's own controlled-validation phase — could not execute any live interaction testing, which the template treats as blocking on its own (AU-009).

Status: **Wave 3 COMPLETE.** Observability (OB), Backup & Recovery (BR), and Release & Deployment (RD) audits ingested below (all three completed via subagent after the rate limit cleared).

**Wave 3 specialist verdicts:** Observability **NO-GO** (4 Critical findings — the most severe verdict of the audit) · Backup & Recovery **NO-GO** · Release & Deployment **NO-GO** (13 of 14 mandatory launch gates fail).

Key new evidence: Backup & Recovery independently *traced* (not inferred) that replaying the full migration sequence against an empty Postgres database halts partway through, directly confirming DI-001/DI-005's schema-recoverability concern by execution-order analysis rather than static comparison alone — the database genuinely cannot be rebuilt from this repository if lost. Release & Deployment found two new, purely mechanical release blockers independent of everything else: a static build number (`1.0.0+1`) with no increment process, which alone guarantees store rejection on any second upload; and a non-functional "Privacy Policy" text label with no actual link, a near-certain App Store/Play Store submission blocker for a health-data app. Observability confirmed, for four realistic failure scenarios (AI endpoint fully broken, an RLS data leak, a recurrence of the exact DI-002 incident, a launch crash), that **none would currently be detected by the team through any in-app or in-repo mechanism** — discovery would depend entirely on a user complaint in every case.

Status: **Wave 2 COMPLETE.** Functional QA (FQ), Performance (PF), and Reliability & Resilience (RR) audits are ingested below (performed directly by the lead auditor after all three delegated Wave-2 sub-agents failed on a session-wide rate limit before producing output — see note in each domain's findings file). Wave 1 status follows.

**Wave 2 specialist verdicts:** Functional QA CONDITIONAL GO · Performance CONDITIONAL GO · Reliability & Resilience **NO-GO** (two RR1/High findings, both confirmed to be the same systemic silent-failure pattern independently found by four other Wave 1 audits — see `ROOT-005` below, now the most cross-corroborated defect in the audit).

Status: **Wave 1 COMPLETE.** Security (SEC), Code Quality (CQ), Dependencies & Configuration (DC), API & Backend (AB), and Database & Data Integrity (DI) audits are all complete and ingested below. This file will be updated (not renumbered) after each subsequent wave. Finding IDs are never renumbered per master §16/§66.7.

**Wave 1 specialist verdicts:** Security NO-GO · Code Quality CONDITIONAL GO · Dependencies/Config NO-GO · API/Backend NO-GO · Database & Data Integrity NO-GO. Per master §42 (Specialist Verdict Propagation) and §19 (Blocker Propagation), any mandatory-audit NO-GO makes overall master GO impossible until resolved — four independent NO-GOs already guarantee the master verdict cannot be GO. Waves 2–6 continue per plan to produce a complete evidence baseline and remediation plan; the master verdict itself is finalized only in `00_08_FINAL_PRODUCTION_READINESS_REPORT.md`.

Native severities are preserved verbatim from each specialist register. The "Master class" column applies the master's normalization (§18): **BLOCKER** = specialist critical or explicit launch-blocking high; HIGH = serious/conditional; MEDIUM = bounded; LOW = backlog; INFO = observation.

---

## BLOCKER-class findings (launch-blocking)

| Finding | Audit | Native severity | Master class | Summary | Status |
|---|---|---|---|---|---|
| SEC-001 | Security | SEC0 Critical | **BLOCKER — 2 of 3 closure conditions met, re-confirmed (AI Security wave, 2026-09-05)** | Live Gemini API key bundled into every release binary via `.env`-as-asset; called directly from client with no server-side gate (3 of 4 AI features). **All 4 AI features call server-side Edge Functions exclusively — deployed to production and runtime-validated**: real authenticated calls against all 4 deployed functions returned correct Gemini responses; `GEMINI_API_KEY` re-confirmed absent from the current release-mode compiled Flutter asset bundle **and** from the compiled Dart AOT binary itself (`libapp.so` scanned directly for the `AIza…` key pattern — zero matches, a broader check than the prior wave's `.env`-asset-only inspection). Full git history re-searched for any committed `GEMINI_API_KEY` value or `AIza…`-pattern secret — **zero matches**; the one `AIza…` match found anywhere in history is an unrelated Firebase Web API key in the orphaned reference-only `firebase-applet-config.json` (already tracked separately, see `CQ-001`/`DC-008` — not a Gemini credential, out of this finding's scope). **New evidence this wave**: `supabase secrets list` (read-only, safe by design — returns a hash digest, never the plaintext value) shows the deployed `GEMINI_API_KEY` secret's `updated_at` as `2026-09-04T20:27:29Z` — **before** the Edge Function migration commits (`22:55`/`23:54` the same day) — confirming this is the same key that has been in place since before the trust-boundary migration and has never been rotated at any point in this engagement. **Still NOT VERIFIED_CLOSED per explicit instruction**: the exposed old key has **not** been rotated/revoked in Google Cloud Console — attempted this wave (see Phase B), genuinely `CREDENTIAL_ROTATION_OWNER_BLOCKED`: no `gcloud` CLI or other authenticated Google Cloud/Gemini credential-management tooling exists in this session (confirmed, not assumed) | OPEN — deployment + client-artifact verification complete and re-confirmed; rotation remains the sole gate, confirmed `OWNER_BLOCKED` this wave |
| DC-001 | Dependencies/Config | DC0 Critical | **BLOCKER — materially improved, not fully closed** | `.env` bundled as Flutter asset; no build-time environment separation (same root cause as SEC-001, config-management angle). **`GEMINI_API_KEY` — the actual secret this finding is about — no longer lives in `.env` at all.** The broader "should `.env` be a bundled asset" architecture (for the remaining, public-safe `SUPABASE_URL`/`SUPABASE_ANON_KEY` values) is unchanged — that is release-engineering scope (Wave 4), deliberately not undertaken in this Gemini-focused pass | OPEN — secret-exposure risk closed, asset-bundling architecture unchanged |
| DC-004 | Dependencies/Config | DC0 Critical | ~~BLOCKER~~ **VERIFIED CLOSED (identified retroactively, 2026-09-05)** | `runZonedGuarded` swallows `AppEnvironment.load()` startup failures via `debugPrint` only — missing/invalid config causes a silent infinite splash-screen hang, not a visible error | **CLOSED** — `main.dart`'s config-load `try/catch` was moved outside `runZonedGuarded` during the Reliability+Observability wave (a byproduct of wiring `AppErrorReporter` into `main.dart`), reports via `AppErrorReporter`, and renders a visible `_StartupErrorApp` screen instead of hanging on the native splash. Never flagged as closed at the time — corrected during the full-engagement reconciliation below |
| DC-005 | Dependencies/Config | DC0 Critical | **BLOCKER** | Android release build type signed with the **debug** keystore (stock Flutter-template TODO never resolved); cannot be legitimately published | OPEN |
| SEC-003 | Security | SEC1 High | **BLOCKER** | Same Android debug-signing issue, independently confirmed from the security angle | OPEN (duplicate manifestation of DC-005 — see Root Cause register) |
| SEC-004 | Security | SEC1 High | **BLOCKER** | `private_messages` "mark as read" RLS UPDATE policy is row-scoped, not column-scoped — a recipient can rewrite another party's message `content`/`sender_id` via direct PostgREST call | OPEN |
| DC-003 | Dependencies/Config | DC1 High | ~~BLOCKER~~ **VERIFIED CLOSED (Gemini trust-boundary remediation, 2026-09-04)** | `.env.example` omits the actually-required `GEMINI_API_KEY`; documents three AI keys (`OPENAI_API_KEY`, `NISWAH_AI_API_KEY`, `DREAM_INTERPRETER_API_KEY`) that are dead/unused — new builds cannot reliably reproduce a working AI feature from documented config | **CLOSED** — `.env.example` now explicitly documents that `GEMINI_API_KEY` is a server-side Edge Function secret (`supabase secrets set`), never a client value; the three dead AI-key references were removed from `app_environment.dart` entirely (see `CQ-005`), so there is nothing left to misdocument |
| DC-006 | Dependencies/Config | DC1 High | **BLOCKER** | No CI/CD pipeline anywhere; all build/release steps depend entirely on undocumented local developer machine state | OPEN |
| DC-010 | Dependencies/Config | DC1 High | **BLOCKER** | iOS release code signing has no pinned `DEVELOPMENT_TEAM`; relies on whatever Apple ID is logged into the building machine's Xcode | OPEN |
| CQ-003 | Code Quality | CQ1 High | **BLOCKER** (low-effort to clear) | Dead, fully-tested duplicate fiqh rule engine (`FiqhCalculationEngine`) coexists with the live one (`MadhhabRuleEvaluator`), with materially weaker Maliki-school logic; classic "false completeness" risk in a religious-ruling domain | OPEN |
| DI-001 | Database | DI1 High | **BLOCKER — CONFIRMED, deepened (Phase 0, 2026-09-04)** | `schema.sql` and `supabase/migrations/` have repeatedly, materially diverged — 10 of 19 documented tables have no `CREATE TABLE` anywhere in tracked migrations; migrations cannot rebuild the schema from empty. Migration comments admit this already caused real production incidents (silent haid-entry write failures, missing `chat_threads` table). **Independently reproduced this session** via empty-DB migration replay (see `00_05` Phase 0 §UNK-002/007): the "10 undocumented tables" list must be corrected to explicitly include `cycle_entries` — the core cycle-tracking feature table — which has zero `CREATE TABLE` in any tracked migration, more severe than any table in the original enumeration | OPEN — CONFIRMED |
| DI-002 | Database | DI1 High (bordering DI0) | **BLOCKER — partially closed (Silent-Failure Remediation, 2026-09-05)** | Systemic pattern: cycle tracking, pregnancy tracking, and community repositories silently swallow remote write/read failures behind local-only or fabricated demo-data fallbacks. Migration comments confirm this already masked real data-loss incidents on core health-tracking data (haid/cycle logging). **Cycle tracking: fixed** — `saveCycleLog` now returns whether remote sync succeeded, reports failures via `AppErrorReporter`, and the UI shows a non-blocking "not backed up yet" indicator instead of the failure vanishing with zero trace; the `sync_status` column bug (successfully-synced rows stayed permanently `'pending'`) is also fixed. **Community (read paths): fixed** — `getPosts`/`getComments` no longer substitute fabricated fake content on a real `PostgrestException`; they report and propagate to the UI's already-correct error state (see `AB-010`). **`pregnancy_profile`: confirmed already correctly designed** — deliberately throws rather than swallows, per its own code comment; no change needed. **`pregnancy_tracking`/milestones: explicitly NOT touched** — deferred per `W0-002`'s product/data-model decision requirement, unrelated to this fix | OPEN — materially narrowed; `W0-002` scope explicitly excluded |
| DI-004 | Database | UNKNOWN (could be DI0) | ~~BLOCKER~~ **RESOLVED FAVORABLY (Phase 0, 2026-09-04)** | No *tracked* code path ever populates the `users` table, the FK target for 14+ core tables. **Live `auth` schema dump reveals two live-only triggers on `auth.users`**, including `auth_users_create_profile` → `create_user_profile()`, which does populate `public.users` on every signup — this function/trigger exists **only in the live database**, absent from every tracked migration and `schema.sql`. Production is not currently broken. **But this is simultaneously the clearest proof of `DI-001`: critical, safety-relevant provisioning logic exists nowhere in version control** — a rebuild from `supabase/migrations/` alone would silently omit it and reintroduce the exact 100%-write-failure scenario this finding originally worried about, for every new signup, going forward. See `00_05` Phase 0 §UNK-006. | **CLOSED (favorable outcome; underlying drift now feeds `DI-001`/`ROOT-007` instead)** |
| DI-005 | Database | DI1 High | **BLOCKER** | `20260830140000_community_schema_reset.sql` contains `DROP TABLE ... CASCADE` guarded only by a code comment ("confirmed empty"), no enforced precondition — a landmine if ever replayed against non-empty data | OPEN |
| RR-001 | Reliability | RR1 High | **BLOCKER — PARTIALLY_REMEDIATED (Reliability+Observability Remediation, 2026-09-05; pregnancy tracking confirmed dormant, Product Integration wave, 2026-09-06)** | No systematic retry/backoff or connectivity-awareness anywhere in the app — a single transient network failure is terminal, no automatic recovery, compounds DI-002. **Cycle tracking has real, working automatic retry**, genuinely wired to fire at app start/resume via `NiswahHomeShell`. **Pregnancy tracking's repository layer has the identical, fully-tested mechanism** (`PregnancySyncStatus`/`syncPendingMilestones()`), **but this wave confirmed `PregnancyTrackingScreen` — the only possible caller — is intentionally dormant** (superseded by the dashboard's own pregnancy overview, not an unwired bug — see `W0-002`), so there is no live path through which a pending write could ever be created in production today. This is not treated as a remaining recovery gap for this specific path — a write path nothing can trigger cannot have a live recovery deficiency — but it also does not count as a second *reachable* success story the way cycle tracking is; it is code-complete, tested, dormant infrastructure. **`RR-003`'s `PregnancyProfileRepository` silent-failure gap remains genuinely unaddressed** and is the reason this finding cannot close regardless of pregnancy-milestones' status — per explicit instruction, not closing solely because one path is handled while another known gap remains. **Still not app-wide** — community/notifications/`PregnancyProfileRepository` writes (`RR-003`) do not have this mechanism | OPEN — cycle tracking's mechanism is proven and live; pregnancy tracking's is proven but dormant (no reachable trigger); `RR-003` and other write paths remain unaddressed |
| RR-002 | Reliability | RR1 High | **BLOCKER** | App-wide `runZonedGuarded` handler silently discards every uncaught async error for the app's entire lifetime (not just startup, contra DC-004's original framing) via `debugPrint` only; no crash-reporting/`FlutterError.onError` safety net exists at all | OPEN |
| OB-001 | Observability | OB0 Critical | **BLOCKER** | No crash/error-tracking tool exists at any layer (Dart, native iOS, native Android) — a launch-crash on any device subset produces zero team-visible signal | OPEN |
| OB-002 | Observability | OB0 Critical | **BLOCKER** | Same `runZonedGuarded` handler as RR-002/FQ-002, assessed from the observability angle: the single largest blind spot in the app | OPEN (duplicate manifestation of RR-002) |
| OB-003 | Observability | OB0 Critical | **BLOCKER** | `dr-niswah-chat` returns **HTTP 200** on Gemini-call failure with zero logging — if `ROOT-001` is real, 100% AI-feature failure would be completely invisible in any log, dashboard, or error tracker | OPEN |
| OB-004 | Observability | OB0 Critical | **BLOCKER** | Server-side outer exception handler is unlogged, and — more seriously — the urgent pregnancy red-flag `flagged_conversations` audit-trail insert shares this same unguarded block, so a DB-insert failure during a genuine safety-relevant message produces a raw 500 with zero trace of what happened (narrower, and distinct from AB's confirmed positive control that the **client-side** urgent banner still fires independently — this finding concerns the **server-side audit-log entry**, not banner delivery to the user) | OPEN |
| OB-009 | Observability | OB1 High | **BLOCKER** | No app-version tagging exists anywhere at runtime; combined with no feature-flag system and no CI, a bad release cannot be isolated, correlated, or disabled remotely — only a full re-release fixes it | OPEN |
| OB-010 | Observability | OB1 High | **BLOCKER** | An RLS policy bug (e.g. SEC-004-class) manifests as a successful query — no exception, so it is structurally invisible to every error-handling/logging path in the app; no DB-level audit/access logging exists anywhere to catch this by any other means | OPEN |
| BR-001 | Backup/Recovery | BR0 Critical | **BLOCKER — CONFIRMED, worst case (Phase 0, 2026-09-04)** | Supabase backup/PITR plan tier and configuration for the live project is completely unverifiable from source — could mean zero automated backups exist at all. **Confirmed via `supabase backups list` (read-only, CLI-authenticated by release owner):** `pitr_enabled: false`, `backups: []` — zero restorable backups exist today. Combined with `BR-002`'s confirmed migration-rebuild failure, the live database currently has **no recovery path of any kind**. **Backup/Recovery wave (2026-09-05): not independently re-confirmed live** — the recurring Supabase CLI keychain-auth hang blocked every authenticated command this wave; Phase 0's evidence is carried forward with that caveat stated explicitly, not silently assumed unchanged. A full production data-backup strategy design was produced regardless (`00_09` §20 Phase H) — remains `OWNER_ACTION` pending plan-tier/billing access this session doesn't have. | OPEN — CONFIRMED (live re-verification blocked this wave, see `00_09` §20 Phase A) |
| BR-002 | Backup/Recovery | BR0 Critical | **BLOCKER — INDEPENDENTLY REPRODUCED (Phase 0, 2026-09-04; re-confirmed live, Backup/Recovery wave, 2026-09-05)** | Directly traced (not inferred): replaying all migrations against an empty Postgres DB halts partway through — the schema **cannot** currently be rebuilt from this repository alone if the live database were lost (extends DI-001/ROOT-007). **Re-reproduced fresh this wave**, live, from a truly empty local Supabase stack (not a bare-Postgres stub this time): `supabase start` with `supabase/migrations/` present failed outright, mid-replay, on the `prayer_log`→`prayer_entries` rename migration. **The working recovery path around this (the canonical baseline) is proven and documented** (`BR_recovery_runbook.md`) — but the tracked migrations themselves remain broken and unsafe to use for restore | OPEN — CONFIRMED, re-reproduced live this wave; recovery workaround proven and documented |
| BR-004 | Backup/Recovery | BR1 High | **BLOCKER — PARTIALLY_REMEDIATED (Backup/Recovery wave, 2026-09-05)** | No disaster-recovery runbook, RPO/RTO target, or restore-testing process exists anywhere, confirmed by exhaustive search. **`BR_recovery_runbook.md` (new) is a real, usable-during-an-incident runbook** — failure scenarios, prerequisites, tested/timed restore sequence, validation sequence, decision points/escalation, and now-defined RPO/RTO targets (`00_09` §20 Phase G). **Not `VERIFIED_CLOSED`**: never exercised by anyone but this session, and never tested against the real production project — only an isolated local proxy | PARTIALLY_REMEDIATED |
| BR-005 | Backup/Recovery | BR1 High | **BLOCKER** | If any backup exists, its failure would be silent — extends RR-002's app-wide silent-error pattern to the backup domain specifically. No backup mechanism exists yet to fail silently or otherwise — this finding activates once a real backup is implemented; the proposed strategy (`00_09` §20 Phase H) includes explicit backup-verification/alerting specifically to prevent this finding recurring at that point | OPEN |
| RD-006 | Release/Deployment | RD1 High | **BLOCKER** | No build-number increment process — `1.0.0+1` is static; both app stores mechanically reject any re-upload with an unchanged build number | OPEN |
| RD-007 | Release/Deployment | RD1 High | **BLOCKER — PARTIALLY_REMEDIATED (Privacy/Compliance wave, 2026-09-05)** | No CHANGELOG/store metadata anywhere; the in-app "Privacy Policy" text on the sign-in screen has no `onTap`/`url_launcher` wiring and no policy document exists anywhere — near-certain store-submission blocker for a health-data app. **The link now works and a real, factual policy document exists** (`PrivacyPolicyScreen`, in-app) — reachable from sign-up and Profile → Privacy Settings. **Not `VERIFIED_CLOSED`**: app-store submission forms require a publicly-hosted, externally-linkable URL, which an in-app screen cannot provide — registered `OWNER_BLOCKED` for that specific publishing step; CHANGELOG/store metadata remain untouched | PARTIALLY_REMEDIATED — in-app link fixed; public hosting is `OWNER_BLOCKED` |
| RD-009 | Release/Deployment | RD1 High | **BLOCKER** | No feature-flag system and no rollback path faster than a full store-review cycle exist, beyond one narrow, undocumented backend kill-switch verified for exactly one feature (Dr. Niswah chat) and not confirmed to generalize | OPEN |
| PC-001 | Privacy | PC0 Critical | **BLOCKER — VERIFIED_CLOSED (Privacy/Compliance wave, 2026-09-05)** | Sign-up consent checkbox is cosmetic — its state is never read by any sign-up button; health/pregnancy/religious/location data collection is not gated by consent at all. **Fixed**: consent state lifted to the screen that actually performs auth calls and now genuinely gates all three entry points (email, phone, Google) — an unchecked box blocks the flow with a clear bilingual message before any Supabase call is made. **Validated via a real widget test** exercising the actual buttons, not just code review | **VERIFIED_CLOSED** |
| PC-002 | Privacy | PC0 Critical | **BLOCKER — VERIFIED_CLOSED (Privacy/Compliance wave, 2026-09-05)** | No account-deletion capability exists anywhere in the app; DB cascade logic is correctly designed but no code path ever calls it, and no alternate channel exists. **Fixed**: `AuthRepository.deleteAccount()` (new) calls the existing `delete_my_account()` RPC and then explicitly clears the local session — a real bug (the RPC alone doesn't clear the client's local session/auth-state stream) caught and fixed in the process. Wired into Profile with a destructive-action confirmation dialog, loading state, and accurate messaging. **Validated end-to-end against a live-schema-derived isolated environment**: session confirmed cleared (`currentUser`/`currentSession` both null), re-login confirmed failing with `invalid_credentials` | **VERIFIED_CLOSED** |
| PC-003 | Privacy | PC1 High | **BLOCKER — PARTIALLY_REMEDIATED (2026-09-05)** | Health/pregnancy context reaches Google Gemini via two confirmed code paths with zero in-app disclosure to users that a third party processes it. **Disclosure half fixed**: the new privacy policy explicitly names Google/Gemini and describes what's sent. **Minimization was audited and found already correct** — `dr-niswah-chat` (the only feature reading health data) sends only derived values via an explicit column selection, never raw dates, never chat history; the other 3 AI features send zero profile/health context at all — confirmed by reading the actual server-side code, not assumed | PARTIALLY_REMEDIATED — disclosure fixed; minimization confirmed already sound |
| PC-004 | Privacy | PC1 High | **BLOCKER — PARTIALLY_REMEDIATED (2026-09-05)** | No privacy-policy document exists at all — same root defect as RD-007, registered in the privacy domain. **In-app reachability fully fixed**: a real, factual, bilingual policy screen exists and the sign-up link is genuinely tappable (confirmed via widget test — a real `TapGestureRecognizer`, not decorative text). **Not `VERIFIED_CLOSED`**: app-store submission requires a public URL, which this session cannot host — `OWNER_BLOCKED` for that specific step, content itself ready to publish as-is | PARTIALLY_REMEDIATED — in-app reachability fixed; public hosting `OWNER_BLOCKED` |
| PC-008 | Privacy | PC2 Medium | **BLOCKER** | No DPA/subprocessor terms found for either Supabase or Google Gemini — flagged launch-blocking pending contractual/legal review. **Unchanged** — explicitly legal/contractual, outside this session's boundary per the operator's own instruction; a technical processor inventory (data categories, purpose, path) was produced instead (`00_09` §22 Phase J), not a substitute for the legal review this finding actually requires | OPEN |
| AU-001 | Accessibility | AU1 High | **BLOCKER — VERIFIED_CLOSED (Accessibility/UX wave, 2026-09-05)** | 15 of 20 icon-only buttons (password toggle, delete/close actions, onboarding navigation) expose no accessible name to screen readers. **Fixed**: every confirmed site now has a `tooltip:`/`Semantics(label:)`; the one bare `GestureDetector` close button (`cycle_log_form_sheet.dart`) now has a real `Semantics(button:true, label:...)` wrapper. A new shared `NiswahIconButton` primitive (`common_widgets.dart`) is available for future icon-only controls. **Validated via Flutter's real semantics tree** (`tester.ensureSemantics()`/`getSemantics()`, not just code reading) across 10 targeted tests | **VERIFIED_CLOSED** |
| AU-009 | Accessibility | Unknown (process) | **BLOCKER — unchanged** | Controlled interaction/screen-reader testing phase never executed for any journey — template treats this as launch-relevant on its own. **Still not executed this wave either** — no live device/emulator/screen-reader tooling is available in this session, the same environmental constraint stated in every prior wave. Automated semantics-tree testing (this wave, see `AU-001`) is meaningfully stronger evidence than static code reading, but it is not live AT/device interaction — this finding's own bar. Per the specialist audit's own decision rule, this alone still independently drives a NO-GO verdict for the Accessibility domain | OPEN |
| PJ-001 | Final User Journey | PJ0 Critical | **BLOCKER** | Traces the exact mechanism behind DI-004: 7+ core tables FK to a bare `public.users` no code path ever writes to; migration history is internally inconsistent about which "users" table is correct | OPEN |
| PJ-002 | Final User Journey | PJ0 Critical | **BLOCKER — PARTIALLY_REMEDIATED (2026-09-05)** | Full data-loss chain traced end-to-end: local-first UI + silent remote-write failure + no cross-device sync + zero "not backed up" warning anywhere — haid history is plausibly local-device-only. **"Zero warning" is fixed**: the UI now shows an accurate, status-specific, bilingual message distinguishing "will back up automatically" (genuinely true now — see `RR-001`) from "could not be backed up, please try again later" (non-retryable). **"Silent remote-write failure" is fixed** for the retryable case (real automatic retry now exists). **"No cross-device sync"** is unchanged — this finding is about real-time multi-device state, a different, larger architectural question, not attempted here | OPEN — the data-loss-with-no-warning defect is closed; cross-device sync remains a separate, unaddressed concern |
| PJ-004 | Final User Journey | PJ0 Critical | **BLOCKER — PARTIALLY_REMEDIATED, code fix complete, not yet deployed/live-verified (2026-09-05)** | On red-flag chat backend failure, three persistence failures stack: audit log, user's own message, AND the reassuring banner shown to her are all lost — the entire safety exchange vanishes on next app open. **Independently re-verified via direct code trace against the currently-deployed `dr-niswah-chat` (not inferred from the earlier Gemini remediation, which never touched this code path)**: confirmed the exact defect is still present today — any one of the three writes failing throws uncaught to the outer handler, aborting the whole request with a raw 500 before Gemini is even called in some cases. **Fixed in this pass**: each of the three writes (`flagged_conversations` insert, user message insert, assistant message insert) is now independently try/caught and logged; critically, the computed reply — including the safety banner — is now returned to the client regardless of whether any of the three persistence writes succeeds, and a correlated zero-tolerance log line fires if any part of an urgent exchange didn't fully persist. **Not yet deployed to production and not yet live-tested against a forced failure** — this is a code-level fix awaiting the same deploy+verify cycle the Gemini remediation went through, deliberately not done in this pass per the stop-condition boundary (deployment) | OPEN — code fix complete and independently verified via trace; deployment + live failure-injection test still required before this can move toward closure |
| PJ-005 | Final User Journey | PJ1 High | **BLOCKER** | Pinpoints CQ-007's exact live trigger: a per-screen-open session check silently and repeatedly drops a user into fabricated conversations the moment her session dies mid-use, no re-auth prompt ever shown | OPEN |
| PJ-006 | Final User Journey | PJ0 Critical | **BLOCKER — PARTIALLY_REMEDIATED (root-cause visibility only, 2026-09-05; re-checked, unaffected, W0-002 wave, 2026-09-06; re-checked again, still unaffected, Product Integration wave, 2026-09-06)** | Doctor's Report red-flag section is architecturally incapable of distinguishing "nothing happened" from "every safety record failed to save" — a reviewing doctor cannot know the report is incomplete. **Root cause's silence is fixed** — a `flagged_conversations` insert failure is now always logged (`PJ-004`'s fix), so an operator can detect this happened. **The Doctor's Report feature itself is unchanged** — it still has no way to represent "this section may be incomplete" in its own output. **Re-traced this wave, per explicit instruction, given `W0-002`'s pregnancy-schema fix**: confirmed `doctor_report_insights_engine.dart` aggregates from `pregnancy_profile` only — it has never read `pregnancy_milestones`/the `pregnancy_tracking` feature at all, and still doesn't. `W0-002`'s fix genuinely does not touch this finding's aggregation path in any way; **correctly preserved `OPEN`, not closed merely because pregnancy tracking is now structurally valid elsewhere**. **Re-confirmed once more this wave (Product Integration + RR-001 Closure, 2026-09-06)**: `PregnancyTrackingScreen`'s classification as dormant/superseded (see `W0-002` row) does not change milestone reachability, report completeness, or the aggregation source in any way — there was no material change for this finding to react to, so it remains untouched and `OPEN` by the charter's own instruction not to redesign or close it absent direct evidence of a completeness change | OPEN — operator-side visibility restored; the report's own architectural gap is untouched; confirmed unaffected by both the pregnancy data-model fix and this wave's dormant-feature classification |
| AB-001 | API/Backend | AB1 High | ~~BLOCKER~~ **VERIFIED CLOSED (Phase 0, 2026-09-04)** | Gemini endpoint/request/response shape used in both the edge function and the client service does not match Google's documented Gemini API — if wrong, all 3 AI features are silently non-functional in production | **CLOSED** — live smoke test against the exact endpoint/shape confirmed a real, functional API (`v1beta/interactions`, a newer Gemini surface); full round-trip succeeded with real text output matching the client's parser. See `00_05` Phase 0 §UNK-001. |
| CQ-010 | Code Quality | CQ2→conditionally CQ0/CQ1 | ~~BLOCKER~~ **VERIFIED CLOSED (Phase 0, 2026-09-04)** | Same finding as AB-001, independently derived by a second auditor | **CLOSED** — same evidence as AB-001; see `00_05` Phase 0 §UNK-001. |
| AB-002 | API/Backend | AB1 High | **BLOCKER — PARTIALLY_REMEDIATED, rate-limit half now CODE_COMPLETE / LOCALLY_VERIFIED (AI Security wave, 2026-09-05)** | AI Advisor / Dream Interpreter call Gemini directly from client, no rate limit/cost cap/moderation; same root cause as SEC-001. **The "direct from client" half is fixed and deployed** — all 4 AI features now call Edge Functions exclusively. **The "no rate limit/cost cap" half**: the in-memory limiter proven ineffective in production has been replaced with a durable Postgres-backed limiter, fully implemented and validated against an isolated local stack including a real 25-concurrent-request HTTP load test against the actual `ai-assistant-chat` function (15 allowed, 10 correctly rejected, 0 unexpected). **See `W1-001`** for full evidence. **Not production-deployed** — the migration has not been applied to the live database, per this wave's explicit production-safety boundary | PARTIALLY_REMEDIATED — direct-call defect closed; rate-limiting fix ready, awaiting production deployment approval |

## HIGH / MEDIUM-class findings (non-blocking but material)

| Finding | Audit | Native severity | Master class | Summary |
|---|---|---|---|---|
| SEC-002 | Security | SEC2 Medium | ~~MEDIUM~~ **VERIFIED CLOSED (2026-09-04)** | Dead secret-validation code for unused AI-key vars creates false assurance — `_validateSecretConfig` and the `openAiApiKey`/`niswahAiApiKey`/`dreamInterpreterApiKey`/`aiApiKeys` getters removed entirely from `app_environment.dart`; zero remaining call sites |
| SEC-005 | Security | SEC2 Medium | MEDIUM — **PARTIALLY_REMEDIATED, CODE_COMPLETE / LOCALLY_VERIFIED (AI Security wave, 2026-09-05)** | No rate limiting on the Gemini-backed edge function beyond JWT validity. An in-memory limiter was added and deployed; live load-testing (28 requests) showed it does not enforce in production. **Replaced with a durable, Postgres-backed atomic limiter, fully implemented and validated** — see `W1-001`/`AB-002`. Not yet production-deployed |
| SEC-006 | Security | SEC2 Medium | ~~MEDIUM~~ **VERIFIED CLOSED (2026-09-04)** | Direct-Gemini fallback path can leak pregnancy/health context to Google and skips clinical audit logging to `flagged_conversations` — the fallback path is removed entirely; `chat_view_model.dart` now throws a clear "temporarily unavailable" error if the `dr-niswah-chat` backend is unreachable rather than downgrading to a client-side call; `dr_niswah_persona.dart` (which built the pregnancy context client-side for that path) deleted |
| DC-002 | Dependencies/Config | DC2 Medium | MEDIUM | `APP_ENV` loaded/validated but has zero effect anywhere in the app (dead config) |
| DC-007 | Dependencies/Config | DC2 Medium | MEDIUM | No Flutter/Dart SDK version pinning mechanism |
| CQ-002 | Code Quality | CQ2 Medium | MEDIUM | Live fiqh rule constants numerically match `haidfigh.md`, which is marked "pending scholar review," with no code-level citation |
| CQ-004 | Code Quality | CQ2 Medium | ~~MEDIUM~~ **VERIFIED CLOSED (2026-09-04)** | Inconsistent AI-key trust model across features (documented architecture, but escalates to Security severity) — all 4 AI features now use the identical server-side Edge Function pattern; no remaining inconsistency |
| CQ-007 | Code Quality | CQ2 Medium | MEDIUM | Private messaging silently falls back to fabricated "demo" conversations whenever no authenticated session exists — including an unexpected session drop, not just deliberate guest browsing |
| CQ-009 | Code Quality | CQ2 Medium | MEDIUM — **half closed (2026-09-05)** | No logging framework; 14 call sites in 2 core repositories swallow Supabase errors via `print()`, invisible in release builds. `user_profile_repository.dart`'s 6 `print()` sites replaced with `AppErrorReporter.report()` (its existing null-return contract to callers preserved — the UI already correctly checked for `null`). `cycle_log_repository.dart`'s sites left untouched — that file is dead code with zero callers (see `CQ-003`), fixing its logging would have no user-facing effect; deletion (per `CQ-003`'s own remediation) is the correct closure path, not logging it |
| AB-003 | API/Backend | AB2 Medium | MEDIUM | No idempotency key on `dr-niswah-chat` message sends — retries can duplicate `chat_messages` rows |
| AB-004 | API/Backend | AB2 Medium | LOW-MEDIUM | Server-side Gemini call has no explicit timeout/AbortController |
| AB-006 | API/Backend | AB2 Medium | MEDIUM | No max-length/shape validation on `dr-niswah-chat` request body |
| AB-007 | API/Backend | AB2 Medium | MEDIUM | Generic error handler can leak internal exception text to the client |
| AB-008 | API/Backend | AB2 Medium | MEDIUM — **PARTIALLY_REMEDIATED, CODE_COMPLETE / LOCALLY_VERIFIED (AI Security wave, 2026-09-05)** | No rate limiting on `dr-niswah-chat` itself (server-side, still uncapped). Same in-memory limiter added (exempting red-flag/urgent messages, which must never be blocked); same production evidence that it does not reliably enforce. **Replaced with the new durable limiter — the red-flag exemption was directly re-tested and confirmed still bypasses the limiter correctly even when the user's counter is pushed to 20/15 (far over quota): the safety banner and full reply were returned, HTTP 200, not blocked.** See `W1-001`. Not yet production-deployed |
| AB-010 | API/Backend | AB2 Medium | MEDIUM — **fake-data half VERIFIED CLOSED; shared-mapping half PARTIALLY_REMEDIATED (2026-09-05)** | Four data-layer repositories use four different, non-shared error-handling strategies; `CommunityRepositoryImpl` substitutes hardcoded fake posts on a real DB error. **The fake-data-on-real-error defect is fixed** (unchanged from the Gemini-remediation-pass finding). **The "four different non-shared strategies" half is now partially addressed**: `mapRepositoryError()` (a genuine shared classifier — see `OB-007`) now exists and is adopted by `CommunityRepositoryImpl.getPosts`/`getComments` and `CycleTrackingRepositoryImpl`'s remote calls — 2 of the 4 originally-cited repositories now share one mechanism instead of independently inventing their own. `PregnancyProfileRepository` was separately confirmed already well-designed (throws deliberately, no swallowing) and doesn't need this. `user_profile_repository.dart`'s null-return contract was deliberately preserved as-is (a different, also-reasonable pattern, not unified into the throw-based classifier in this pass) | OPEN — meaningfully narrowed, not fully unified |
| DI-003 | Database | DI2 Medium | MEDIUM | `private_conversations` UNIQUE constraint enforces ordered-pair, not unordered-pair, uniqueness — TOCTOU race can create duplicate/split conversation threads |
| DI-011 | Database | DI2 Medium | MEDIUM | `cycle_entries` has no `UNIQUE(user_id, date)` (unlike the analogous `wellbeing_logs`); merge logic keys by row id, not date, so same-day duplicate/contradictory entries are possible |
| FQ-002 | Functional QA | P2 Medium | MEDIUM | App-wide silent async-error handler (same code as RR-002/DC-004) means undetected functional regressions could ship invisibly |
| RR-003 | Reliability | RR2 Medium | ~~MEDIUM~~ **VERIFIED CLOSED (2026-09-05)** | Notification scheduling silently no-ops if init failed; underlying plugin calls unguarded by try/catch. `showNow`/`scheduleAt`/`scheduleDaily` now report via `AppErrorReporter` both when called before successful initialization and when the underlying plugin call itself throws; all three plugin calls now wrapped in try/catch. `cancel`'s pre-init no-op deliberately left silent (best-effort cleanup of something that was never scheduled — not a data-loss risk) |
| RR-004 | Reliability | RR2 Medium | MEDIUM | No coherent offline/degraded-network product decision — behavior differs by accident across features (local-first vs. hard error vs. fabricated demo data) |
| PF-001 | Performance | PF2 Medium | MEDIUM | Ten sequential, unparallelized `await` calls block first frame at startup |
| PF-002 | Performance | PF2 Medium | MEDIUM | Full IANA timezone database loaded synchronously on startup critical path |
| PF-003 | Performance | PF2 Medium | MEDIUM | PDF report generation not offloaded from the UI isolate (mitigated by existing loading-state UX) |
| OB-005 | Observability | OB1 High | MEDIUM | Client-side AI call sites (`AiAdvisorService`, dream interpreter, chat fallback) have no logging on failure — compounds OB-003 |
| OB-006 | Observability | OB1 High | MEDIUM — **PARTIALLY_REMEDIATED (corrected 2026-09-05)** | No logging framework app-wide; 18 `print`/`debugPrint` sites against 129 catch blocks total; the exact DI-002 incident code path is unchanged today. **Update:** a real Sentry (`sentry_flutter`) integration now exists — `AppErrorReporter.onReport` reaches a real, owner-provided Sentry project, verified via one controlled test exception that received a real, server-confirmed event id. **Not yet `VERIFIED_CLOSED`**: that verification ran in the `development` environment (a local test run), not a compiled release/staging build — the original `OB` remediation plan's own closure bar (`OB_remediation_plan.md` R2-1) requires confirming this "in a test/staging environment." One release/staging build emitting a real event (Release Engineering wave, item 8) is the remaining condition |
| OB-007 | Observability | OB2 Medium | MEDIUM — **PARTIALLY_REMEDIATED (2026-09-05)** | `Failure` exception hierarchy is message-only — a future logging integration would need to touch ~129 catch sites individually, not one shared shape. **The hierarchy itself is fixed**: `Failure` now carries `cause`, `stackTrace`, `retryable`, and `context` (all optional, fully backward-compatible — verified via a dedicated test that the existing 2-argument construction used at ~120+ call sites still compiles and works unchanged). A new shared `mapRepositoryError()` classifier produces correctly-populated `Failure`s from a caught error in one call, tested against real `PostgrestException`/`AuthApiException`/`AuthRetryableFetchException` instances (10 tests, one of which caught a genuine classifier bug — an auth-named exception that actually means "retry me" — before it shipped). **Adopted so far in**: `cycle_tracking_repository_impl.dart`, `community_repository_impl.dart` (2 of the originally-cited repositories). **Not yet adopted in the other ~127 catch sites** — this was always described as a staged rollout in the original `OB` remediation plan, not a single-pass migration | OPEN — shared mechanism built, tested, and proven; adoption is partial, not complete |
| OB-008 | Observability | OB2 Medium | MEDIUM | No analytics/telemetry SDK at all — removes even the weakest secondary/proxy detection signal |
| OB-011 | Observability | OB2 Medium | LOW | No application-level dashboard; whether Supabase's own default dashboard is watched by anyone is UNKNOWN |
| OB-012 | Observability | OB2 Medium | LOW | Inconsistent logging style across the few sites that do log; confirms no misleading placeholder monitoring exists (narrow positive) |
| BR-003 | Backup/Recovery | BR1 High | MEDIUM | Destructive `DROP TABLE` migration (DI-005) has no backup-before evidence, no rollback script, no restore ever demonstrated |
| BR-006 | Backup/Recovery | BR2 Medium | MEDIUM — **PARTIALLY_REMEDIATED (Local Sensitive Storage wave, 2026-09-05)** | On-device health/religious data has no deliberate backup design — only inherits undocumented OS-default backup behavior (privacy cross-reference). **Now documented, not undocumented**: Android's OS-default behavior is `allowBackup="false"` (excludes everything, confirmed), and iOS's default was checked directly against the platform-secure-storage package's actual option values (not assumed) and found to permit device-to-device migration via an encrypted local backup restore — now explicitly hardened to `unlocked_this_device` so restored data simply reads as absent on a new device rather than migrating. **Not fully closed**: this documents/hardens the *encrypted* cycle/prayer/pregnancy categories specifically; other local preference-level `SharedPreferences` keys remain on OS-default behavior, undocumented at that granularity | PARTIALLY_REMEDIATED |
| BR-007 | Backup/Recovery | BR2 Medium | ~~LOW~~ **VERIFIED_CLOSED (Wave 0, 2026-09-04; re-confirmed Backup/Recovery wave, 2026-09-05)** | No IaC/export of Supabase project-level config; reconstruction depends on undocumented dashboard access. **Resolved**: Storage schema confirmed to have zero application buckets/policies both times checked — Storage is not in application use, so this finding's underlying concern doesn't apply. (This closure was recorded in Wave 0's own output table but never propagated to this row until now — corrected as part of the Backup/Recovery wave's reconciliation.) |
| BR-008 | Backup/Recovery | BR2 Medium | LOW — **PARTIALLY_REMEDIATED (Backup/Recovery wave, 2026-09-05)** | No evidence any backup or migration has ever actually been restored (Golden Rule violation) — even if backups exist, they are unverified. **A real restore has now been demonstrated twice** (Wave 0, and again this wave using the real local Supabase Auth service rather than a hand-stubbed approximation) — but only ever against an isolated local environment, never the real production project or a real platform backup (none exists to test). Not `VERIFIED_CLOSED` — a logical-schema restore working does not close a finding whose original intent was about *production* backup verification |
| RD-004 | Release/Deployment | RD2 Medium | MEDIUM | No Flutter/Dart SDK version pinning (cross-ref DC-007, consolidated here) |
| RD-008 | Release/Deployment | RD2 Medium | LOW | 2-commit squashed git history, single author, no PR/review trail — cannot verify this candidate was an intentional, reviewed assembly |
| RD-010 | Release/Deployment | RD3 Low | LOW | No key-custody/backup plan for the eventual release keystore (forward-looking, moot until RD-001 closes) |
| PC-005 | Privacy | PC2 Medium | MEDIUM — unchanged | Onboarding step literally labeled "Privacy" is actually just a community-anonymity toggle — labeling/expectation mismatch. Not addressed this wave (out of scope); the "Privacy Settings" profile section now also contains a real Privacy Policy link (a partial, incidental improvement), but the underlying mismatch this finding describes is unresolved |
| PC-006 | Privacy | PC2 Medium | MEDIUM — **PARTIALLY_REMEDIATED (2026-09-05; pregnancy-milestone gap closed, W0-002 wave, 2026-09-06)** | "Data Export" only produces 4 curated PDF reports, not a full personal-data export. **A real, working raw JSON export now exists** alongside the PDFs — RLS-verified own-data-only. **`pregnancy_milestones` now included** in the export (added this wave, once `W0-002` made the table real) — confirmed via the same query pattern the export screen uses, against a real isolated Postgres instance; a stale doc-comment claiming both prayer and pregnancy data were excluded was also corrected (prayer had already been included since the `W0-003` wave, the comment was simply never updated). **Still explicitly partial**: excludes the internal `flagged_conversations` safety log by design (service-role-only, not user-owned content), not a polished file/share flow | PARTIALLY_REMEDIATED |
| PC-007 | Privacy | PC2 Medium | MEDIUM — core finding unchanged; addendum now VERIFIED_CLOSED (Local Sensitive Storage wave, 2026-09-05) | No retention *period* defined for any data category, including precise GPS location cached indefinitely — **core finding still accurately OPEN, not invented**. Addendum (local device data surviving account deletion): **fixed and tested this wave** — `AuthRepositoryImpl.deleteAccount()` now clears cycle/prayer/pregnancy local data after the RPC succeeds, with per-category failure reporting and startup retry for any partial failure. See `00_09` §23 |
| PC-010 (**new**) | Privacy | Newly discovered | **HIGH — VERIFIED_CLOSED (Local Sensitive Storage wave, 2026-09-05)** | Cross-user local data leak: cycle/prayer/pregnancy `SharedPreferences` caches used a fixed **global** key with zero per-user scoping — a second person signing in on the same device after the first signed out could read the first person's cached health data. Cycle tracking had zero filtering at any layer (worst case); prayer tracking had only a fragile client-side filter over a still-shared cache; pregnancy tracking had zero filtering. **Fixed**: every local storage key is now scoped by the resolved current-user id internally (`SecureLocalStore.currentUserId()`), so a different signed-in user resolves an entirely different key — no explicit wipe-on-logout required, the leak is structurally impossible. **Validated via a mandatory dedicated test**: User A writes local data → simulated logout → User B signs in → User B reads back empty; User A's own data unaffected. Run for both cycle and prayer tracking. See `00_09` §23 Phase A/G | **VERIFIED_CLOSED** |
| PC-009 | Privacy | PC2 Medium | MEDIUM — unchanged | No age-gate/minimum-age mechanism exists anywhere for a plausibly-minor-used health app. Not addressed this wave — out of scope |
| AU-002 | Accessibility | AU2 Medium | LOW — **VERIFIED_CLOSED (Accessibility/UX wave, 2026-09-05)** | Dashboard phase-timeline stepper lacks semantic grouping (bounded — ring above it is correctly labeled). **Fixed**: `_PhaseNode` now exposes one merged `Semantics` label per phase (e.g. "Haid, 3 days, current phase") via `excludeSemantics`, instead of fragmented value/unit/label/marker text nodes |
| AU-003 | Accessibility | AU2 Medium | MEDIUM — **VERIFIED_CLOSED (Accessibility/UX wave, 2026-09-05)** | `tahara`/`nifas` fiqh-state colors fail WCAG AA 4.5:1 as text color, confirmed used as real text color. **Fixed for the confirmed defect**: a new `AppColors.taharaText` (hue-preserving darkened variant, recalculated to 4.63:1) applied at the two confirmed small-text usage sites (dashboard "Mental state check-in" label, Insights `_PredictionValue`); base `AppColors.tahara`/`nifas` left untouched for icon/border/background uses, which already clear the applicable 3:1 UI-component bar — preserves visual identity elsewhere per the wave's own constraint. Affected goldens re-captured |
| AU-004 | Accessibility | AU2 Medium | LOW — **VERIFIED_CLOSED (Accessibility/UX wave, 2026-09-05)** | Symptom-severity chips conveyed by color-alpha alone, no text/numeric indicator; touch target below minimum. **Fixed**: chips now carry a non-color dot indicator (filled/outline, count = severity) plus a `Semantics` label announcing the level ("Cramps, mild"), and vertical padding increased so the rendered height clears 44dp |
| AU-006 | Accessibility | AU2 Medium | LOW — **VERIFIED_CLOSED (Accessibility/UX wave, 2026-09-05)** | Fixed-dimension dashboard widgets not proofed against large OS text-scale settings. **Fixed**: `_PhaseNode`'s value/unit text and the cycle ring's headline/subtitle now wrap in `FittedBox(fit: BoxFit.scaleDown)`, matching the treatment already used for "YOU ARE HERE". **Validated via a real widget test** rendering the dashboard at 1x/1.5x/2x/3x text scale in both languages and asserting no layout-overflow exception (not just code review) |
| AU-007 | Accessibility | AU2 Medium | LOW — **PARTIALLY_REMEDIATED (Accessibility/UX wave, 2026-09-05)** | No centralized localization catalog; only 24% of screens have any automated RTL visual-regression coverage. **Partially addressed**: a new automated regression test scans every `lib/` source file for the exact physical-`EdgeInsets` pattern that causes RTL bugs, failing the suite if one is (re)introduced — the "add a lint rule" half of the specialist's own remediation plan (R6b). **The ARB/`gen-l10n` migration and expanding golden coverage past 24% remain explicitly out of scope** — correctly deferred as a larger, separate initiative per the specialist's own R6(c) recommendation, not attempted here |
| AU-005 | Accessibility | AU3 Low | LOW — **PARTIALLY_REMEDIATED (Accessibility/UX wave, 2026-09-05)** | Inconsistent form-error UX: `cycle_log_form_sheet.dart` surfaces save/validation errors only via a global `SnackBar`, while `community_composer_sheet.dart` uses a field-anchored validator. **Partially addressed**: the cycle-log form has no fields that are actually required (confirmed — nothing to field-validate), so a `Form`/`validator` migration was judged manufactured complexity with no real defect to fix; instead, the save-failure `SnackBar` now carries a visible error icon + distinct color, a non-text signal distinguishing it from the ordinary pending/synced outcomes it previously looked identical to |
| AU-008 | Accessibility | AU3 Low | LOW — **VERIFIED_CLOSED (Accessibility/UX wave, 2026-09-05)** | Confirmed physical-`EdgeInsets` RTL padding bug in `common_widgets.dart`'s `StatCard` (live on the golden-tested Insights screen) and dead-code `NiswahListTile`. **Fixed**: both changed to `EdgeInsetsDirectional`; a third, previously-unconfirmed instance found and fixed in the same sweep (`cycle_tracking_screen.dart`'s dead `_Legend` widget). A regression test (see `AU-007`) now guards against recurrence app-wide |
| AU-010 | Accessibility | AU4 Observation | LOW — **VERIFIED_CLOSED (Accessibility/UX wave, 2026-09-05)** | No screen ever explicitly excludes decorative elements from the semantics tree. **Addressed for every confirmed instance found this wave**: `_PhaseNode`'s decorative "current" marker dot, the cycle-log sheet's close icon, symptom chips, the nav bar, and several others (see `AU-011`) now correctly exclude redundant/decorative descendant semantics via `excludeSemantics`/`container: true` |
| AU-011 (**new**) | Accessibility | Newly discovered | **HIGH — VERIFIED_CLOSED (Accessibility/UX wave, 2026-09-05)** | Discovered while adding semantics-tree tests for `AU-001`, not by static reading: several custom `Semantics(label: ...)` wrappers lacked an `excludeSemantics`/`container: true` boundary, so Flutter silently **merged** them with sibling/descendant text into one composite node. In the cycle-log sheet specifically, this meant the close button's `Semantics` merged with the header's title/subtitle text into a single node carrying **both** the informational text **and** the close action — a screen-reader user activating what reads as ordinary header text would have actually triggered "close the sheet" (a real, confusing, potentially data-losing mis-activation, not merely an audio-redundancy issue). The same class of bug (redundant but not misleading label duplication) was independently found in the symptom-severity chips, the bottom navigation bar, the dashboard notifications button, the wellbeing check-in sheet's close button, the community engagement bar, cycle-calendar day cells, and the cycle ring. **Fixed in every confirmed instance**; the wrong-action defect specifically validated via a live semantics-tree dump before and after the fix, not assumed | **VERIFIED_CLOSED** |
| AU-012 (**new**) | Accessibility | Newly discovered | **MEDIUM — VERIFIED_CLOSED (Accessibility/UX wave, 2026-09-05)** | The sign-up consent checkbox is a hand-drawn `InkWell`/`AnimatedContainer` with **no** `Semantics` wrapper at all — a screen reader would not recognize it as a checkbox or announce any checked state, on the one control gating account creation. **Fixed**: wrapped in `Semantics(checked: agreed, label: ..., onTap: ...)`, independently activatable and state-announcing, validated against a live semantics-tree dump and a dedicated test | **VERIFIED_CLOSED** |
| AU-013 (**new**) | Accessibility | Newly discovered | **LOW — VERIFIED_CLOSED (Accessibility/UX wave, 2026-09-05)** | `dr_niswah_chat_screen.dart`'s "SUGGESTED QUESTIONS" label used physical `Alignment.centerLeft` instead of a directional alignment — in Arabic it would stay pinned to the physical left edge instead of following reading direction (the same root cause as `AU-008`, a different confirmed instance). **Fixed**: changed to `AlignmentDirectional.centerStart` | **VERIFIED_CLOSED** |
| PJ-003 | Final User Journey | PJ2 Medium | MEDIUM | A correctly-firing safety banner can render simultaneously with a generic/alarming error state on the same chat screen for the same failed request |

**Note on RD-001/RD-002/RD-003/RD-005:** These are the Release/Deployment audit's formal, re-verified adoption of DC-005/SEC-003, DC-010, DC-006, and DC-001/DC-003 respectively — same underlying defects, not additional distinct blockers. They are not re-listed as separate rows above to avoid double-counting; see `ROOT-003`/`ROOT-004` below, which now also list RD as an owning domain.

## LOW / INFO-class findings

SEC-007, SEC-008, SEC-009, SEC-010 (unknown), SEC-011 (unknown), DC-008, DC-009, DC-011 (unknown), DC-012 (closed/false-positive), CQ-001, CQ-005 (**VERIFIED CLOSED 2026-09-04** — dead `AppEnvironment` AI-key getters removed, see `SEC-002`), CQ-006, CQ-008, CQ-011, CQ-012, AB-005, AB-009, AB-011, AB-012 (**VERIFIED CLOSED 2026-09-04** — trusted-citation filter now enforced server-side in `fiqh-advisor-chat`, not bypassable by a modified client), AB-013, DI-006, DI-007, DI-008, DI-009, DI-010, DI-012, FQ-000 (unknown — golden-test visual diffs not inspected), FQ-001. Full detail in each domain's `*_findings.md`.

---

## Root-Cause Consolidation (preliminary — master §21/§53)

| Root ID | Root cause | Manifestations | Domains | Master priority |
|---|---|---|---|---|
| `ROOT-001` | ~~Gemini API endpoint/request/response shape (`v1beta/interactions`, OpenAI-Responses-style body) does not match Google's documented Gemini REST API — likely hallucinated~~ **DISPROVEN (Phase 0, 2026-09-04):** live smoke test confirms `v1beta/interactions` is a real, functional Gemini API surface (evidently newer than this audit's and this remediation session's training data); full round-trip succeeded end-to-end with real model output correctly parsed by the client. Not a defect. | CQ-010, AB-001 (both CLOSED) | Code Quality, API/Backend | **CLOSED — was Highest, now N/A.** See `00_05` Phase 0 §UNK-001 for full evidence. |
| `ROOT-002` | ~~`GEMINI_API_KEY` is bundled into the compiled app binary (`.env` as Flutter asset) and called directly from the client for 2 of 3 AI features, with no server-side proxy, rate limit, or cost cap~~ **DEPLOYED AND RUNTIME-VALIDATED, ROTATION PENDING (re-confirmed, AI Security wave, 2026-09-05):** all 4 AI features call server-side Edge Functions exclusively, deployed to production; `GEMINI_API_KEY` set as an Edge Function secret; re-confirmed absent from both the release-mode `.env` asset **and** the compiled Dart AOT binary this wave (broader scan than before). Live authenticated runtime tests against all 4 deployed functions passed again this wave, plus a full local isolated-stack re-validation. **Not fully closed** — the previously-exposed key has not yet been rotated/revoked; attempted this wave and confirmed genuinely `CREDENTIAL_ROTATION_OWNER_BLOCKED` (no Google Cloud/Gemini credential-management tooling available in this session — checked, not assumed); rate-limiting (`SEC-005`/`AB-002`/`AB-008`) is now `CODE_COMPLETE`/`LOCALLY_VERIFIED` (`W1-001`) but not yet production-deployed | SEC-001, SEC-002 (closed), SEC-005 (code-complete, locally verified), SEC-006 (closed), DC-001 (partially closed), DC-003 (closed), CQ-004 (closed), CQ-005 (closed), AB-002 (partially remediated, rate-limit code-complete), AB-012 (closed) | Security, Dependencies/Config, Code Quality, API/Backend | **Was Highest — deployment/client-artifact risk eliminated and re-confirmed; sole residual production risk is key rotation (owner-blocked) and deploying the now-ready rate limiter** |
| `ROOT-003` | Release build signing is not production-ready on either platform (Android: debug keystore; iOS: no pinned team) | SEC-003, DC-005, DC-010, RD-001, RD-002 | Security, Dependencies/Config, Release/Deployment | **Highest** — Release/Deployment formally confirms this as a Mandatory NO-GO gate; release cannot be legitimately shipped to either store today |
| `ROOT-004` | No CI/CD, no environment separation, no SDK pinning — release process depends entirely on one developer's local machine state, with zero automated verification of anything (including the app's own substantial test suite, which currently protects nothing at release time) | DC-002, DC-006, DC-007, RD-003, RD-004, RD-005 | Dependencies/Config, Release/Deployment | **Highest** — Release/Deployment audit's own words: the test suite "currently provides zero protective value at release time because nothing runs it automatically" |
| `ROOT-005` | Failure states across the app are silently absorbed rather than surfaced: startup config errors hang the splash screen (DC-004), repository errors collapse to empty/fake data (CQ-009, AB-010, **DI-002**), no retry/backoff exists anywhere (RR-001), and the app-wide uncaught-async-error handler discards everything via `debugPrint` for the app's entire lifetime, not just startup (RR-002, FQ-002, **OB-002** — supersedes DC-004's narrower framing). The Observability audit independently confirmed that for four realistic failure scenarios (AI provider outage, an RLS data leak, a DI-002 recurrence, a launch crash), **none would currently be detected by any mechanism in the app** — not even the exact code path that already caused the documented incident, which is unchanged today (OB-006). The Final User Journey audit then traced this root cause all the way to its real-world, user-visible consequences: haid data plausibly lost with no warning (PJ-002), an urgent chat exchange that self-erases entirely including the safety banner (PJ-004), and a doctor's report structurally unable to reveal it's missing safety data (PJ-006). This is now the single most independently-corroborated systemic pattern in the audit (**nine domains**: Dependencies/Config, Code Quality, API/Backend, Database, Reliability, Functional QA, Observability, Backup/Recovery, Final User Journey). Reliability, Observability, and Final User Journey all issued **NO-GO** primarily on this root cause. | DC-004, CQ-009, AB-010, DI-002, RR-001, RR-002, FQ-002, OB-001, OB-002, OB-003, OB-004, OB-005, OB-006, OB-009, OB-010, BR-005, PJ-002, PJ-004, PJ-006 | Dependencies/Config, Code Quality, API/Backend, Database, Reliability, Functional QA, Observability, Backup/Recovery, Final User Journey | **Highest — single largest cross-audit root cause in the entire engagement, now traced end-to-end to concrete user harm** |
| `ROOT-006` | Orphaned/dead code from an earlier or parallel (Firebase-based) prototype and an abandoned fiqh-engine implementation remain in the tree | CQ-001, CQ-003, CQ-005, DC-008 | Code Quality, Dependencies/Config | **Medium** — CQ-003 escalated to BLOCKER given the religious-ruling domain and test-suite-masks-dead-code risk; the rest is hygiene debt |
| `ROOT-007` | No single verified source of truth for the database schema: `schema.sql` is a hand-maintained, only-partially-reconciled approximation, and `supabase/migrations/` is independently incomplete (significant DDL applied live, outside version control). The Backup & Recovery audit **directly proved** (not inferred) this means the live database cannot currently be rebuilt from this repository if lost — replaying the migrations against an empty DB halts partway through (BR-002). The Final User Journey audit then identified the *specific live mechanism* likely underlying this: 7+ core tables FK to a bare `public.users` table no code path ever populates, and the migration history is internally inconsistent about whether `auth.users` or this bare table is the correct FK target (PJ-001) — meaning this may not be only a disaster-recovery risk but an **ongoing, present-day write-path failure**. Combined with `BR-001`'s unresolved unknown (whether Supabase-side backups even exist for this project), the app currently has **no confirmed disaster-recovery path of any kind** for its core data store. | DI-001, DI-005, DI-006, BR-001, BR-002, BR-003, BR-004, PJ-001 | Database, Backup/Recovery, Final User Journey | **Highest** — process-level root cause behind confirmed past incidents; now additionally proven to remove the last-resort recovery option entirely, and plausibly still live today |
| `ROOT-008` | Total absence of app-version tagging, feature flags, or a general rollback mechanism means a bad release (of any kind — a crash, a broken AI integration, a data-corrupting bug) can only be mitigated by a full app-store re-release, which is itself currently blocked by `ROOT-003`/`ROOT-004`, and — per `ROOT-005` — likely wouldn't be detected quickly enough to react to anyway | OB-009, RD-009 | Observability, Release/Deployment | **High** — compounds every other root cause; there is currently no fast, safe way to react to a launch going wrong |
| `ROOT-009` | Consent and privacy-disclosure infrastructure exists only as UI decoration, not as functioning controls: the sign-up consent checkbox does not gate account creation (PC-001), the linked policy document doesn't exist and its link isn't even wired up (PC-004/RD-007), an onboarding step literally labeled "Privacy" addresses something unrelated (PC-005), and third-party AI data sharing is entirely undisclosed (PC-003) — for an app whose entire purpose is processing health and religious-practice data | PC-001, PC-003, PC-004, PC-005, RD-007 | Privacy/Compliance, Release/Deployment | **Highest** — for a health-data consumer app, functioning consent is a baseline expectation, not a nice-to-have |
| `ROOT-010` | Accessibility semantics are applied inconsistently rather than absently — the team demonstrably knows the correct patterns (well-labeled nav bar, cycle ring, community engagement bar) but applied them to roughly a quarter of icon-only controls and RTL-sensitive widgets, leaving screen-reader and RTL users with an unpredictable experience that varies file-by-file rather than a uniform baseline | AU-001, AU-002, AU-007, AU-008 | Accessibility/UX | **High** — same "some files got it right, most didn't" pattern Code Quality separately flagged (CQ-008) about file-size discipline, suggesting a general consistency/review-process gap rather than a skill gap |

---

## Critical Unknowns Register (preliminary)

| Unknown ID | Area | Why unknown | Blocks GO? |
|---|---|---|---|
| `UNK-001` | Whether the live Gemini endpoint/shape used in `gemini_service.dart` and `dr-niswah-chat` is real | No network/live-credential access authorized in this audit | **YES** until resolved (ROOT-001) |
| `UNK-002` | Whether `supabase/migrations/` have actually been applied, in order, to the live production Supabase project (SEC-010); migration comments show prior schema drift history | No dashboard/CLI access to the live project | **YES** — cross-references Database audit, pending |
| `UNK-003` | Live Supabase dashboard config: Auth rate limits, leaked-password protection, email-confirmation requirement, redirect-URL allow-list, Realtime RLS toggle (SEC-011) | No dashboard access | Partially — material to Security gate |
| `UNK-004` | Live deprecation/advisory status of several pinned dependency versions (DC-011) | No network access for `pub.dev` lookups | NO on its own, but should be closed before final sign-off |
| `UNK-005` | Whether `flutter build --release`/`flutter test` actually succeed | Not executed in Wave 1 (static-analysis-only pass); scheduled for Wave 2 controlled validation | Contributes to Code Quality / Release gates |
| `UNK-006` | Whether `public.users` is actually populated for real accounts (DI-004) — if not, writes to 14+ FK-dependent tables would fail, a failure mode DI-002 would hide from users entirely | No live DB access; requires one read-only query (`SELECT count(*) FROM auth.users u LEFT JOIN public.users pu ON pu.id=u.id WHERE pu.id IS NULL`) or a staging signup test | **YES** — explicit critical unknown per master §3.3/§19 |
| `UNK-007` | Whether the live Supabase schema currently matches either `schema.sql` or the tracked migrations in full (DI-001) | No live `information_schema` access | **YES** — cross-references UNK-002 |
| `UNK-008` | Whether historical silent-failure incidents (DI-002) caused unrecoverable data loss for real users beyond the documented incident window, and how many users were affected | No access to production logs/data | Contributes to Privacy/Data-Integrity severity, not independently blocking if DI-002 remediation proceeds regardless |
| `UNK-005` (RESOLVED) | Whether `flutter test` passes | **RESOLVED THIS WAVE**: executed directly, 254/262 pass. 8 failures are all golden/pixel-diff visual-parity tests (1–7% diff), not logic failures. Whether these 8 represent real UI regressions vs. environment font-rendering variance remains open as `FQ-000` (see Functional QA findings) — not itself a blocking unknown, but unresolved | Closed as a blocking unknown; `FQ-000` remains open as a lower-severity item |

| `UNK-009` | Supabase billing/plan tier and backup/PITR configuration for the live project (BR-001) — Free tier = zero automated backups, Pro = daily, PITR = separate add-on; repo gives no signal which applies | No dashboard/billing access | **YES** — a top-priority unknown; the answer could mean the app has literally no recovery path at all |
| `UNK-010` | Whether any restore has ever actually been demonstrated for any backup or migration (BR-008, Golden Rule) — even if BR-001 resolves favorably, an unexercised backup is unverified | No restore-test evidence exists anywhere | Compounds UNK-009, does not independently block if UNK-009 resolves and a real restore test is then run |
| `UNK-011` | Whether the informal `dr-niswah-chat` kill-switch mitigation (RD-009) generalizes to other Supabase-backed features (cycle tracking, community, messaging, pregnancy tracking) — verified for one feature path only | Would require reading/testing every repository's error-handling path individually | NO independently, but relevant to how much weight `ROOT-008` can be given as a partial mitigation |

This register was extended after Wave 3 (Observability, Backup & Recovery, Release & Deployment ingested). It will be extended again after each subsequent wave.

**Phase 0 update (Remediation Session, 2026-09-04):** All five critical unknowns (`UNK-001`, `UNK-002`, `UNK-006`, `UNK-007`, `UNK-009`) are now resolved — full evidence, verdicts, and affected-finding cross-references in `00_05_UNKNOWN_ASSUMPTION_REGISTER.md`'s "Phase 0 Resolution" section. Summary: `UNK-001` resolved favorably (Gemini integration is real and functional — closes `AB-001`/`CQ-010`, disproves `ROOT-001`). `UNK-002`/`UNK-007` resolved unfavorably and confirmed directly against the live database (migrations were never applied via the tracked mechanism; live schema is a third variant matching neither `schema.sql` nor migrations — deepens `DI-001`/`BR-002`/`ROOT-007`). `UNK-006` resolved favorably (an untracked, live-only trigger does populate `public.users` — closes `DI-004`/`PJ-001` favorably, but is itself new evidence for `DI-001`, since that trigger exists nowhere in version control). `UNK-009` resolved unfavorably, worst case (zero backups exist, PITR disabled — confirms `BR-001`).

**Operational Closure Pass update (2026-09-05):** `ROOT-005` (silent-failure root cause) progressed further — see `00_09` §16 for full evidence. `PJ-004` (urgent AI chat exchange self-erasing) is now `VERIFIED_CLOSED` via a live forced-failure test against an isolated, production-equivalent Supabase stack. `RR-001`, `PJ-002` remain `PARTIALLY_REMEDIATED` (code fixes are real; live/integration-level proof is complete for AI chat but not yet for cycle/haid logging). `PJ-006` and `OB-006` remain `OPEN` by design (product/owner decisions, not code gaps). A new finding, `RR-003`, was discovered during this pass: two of three write call sites for `PregnancyProfileRepository` (`profile_screen.dart::_startNifas`, `dashboard_screen.dart`'s `onLogBirth`) silently discard persistence failures the repository's own design treats as meaningful — this corrects an earlier claim in `00_09` §15 that this path was "confirmed sound," which was not actually verified against the call sites at the time it was written.

**Second Operational Closure Pass update (2026-09-05):** See `00_09` §17 for full evidence. `RR-003` is now fixed (all 3 call sites report and surface failures correctly; the false-success bug in `dashboard_screen.dart`'s `onLogBirth` — an unconditional "success" SnackBar shown even when the sync failed — is corrected) and `VERIFIED_CLOSED`. `PJ-002` is now `VERIFIED_CLOSED`: a live forced-outage test against the isolated stack (stopping and restarting PostgREST, not a database privilege change) proved the pending → retry → synced sequence end-to-end, and in the process surfaced a real, previously-unknown bug — `mapRepositoryError` classified a gateway-level outage (`PostgrestException` with an HTTP status code like `502`/`503` instead of a Postgres SQLSTATE) as non-retryable, permanently failing a save that should have stayed pending for retry. Fixed in `failures.dart`, with a regression test. `RR-001` is now `PARTIALLY_REMEDIATED` with only one remaining gap (pregnancy-tracking's unobserved sync failures, behind the deferred `W0-002`). `OB-006` gained a full Sentry (`sentry_flutter`) integration this pass — `AppErrorReporter.onReport` now reaches a real destination when a DSN is configured — but remains `OPEN` since no DSN exists yet and no live event has been observed, per the operator's explicit closure bar. A fresh synthetic QA account was created for the outstanding `dr-niswah-chat` production normal-path recheck; production's email-confirmation gate blocked automated sign-in again, so per standing instruction this was not retried and awaits owner confirmation in the Dashboard.

**Production normal-path recheck (2026-09-05):** owner confirmed the synthetic QA account; `dr-niswah-chat`'s normal message and red-flag/urgent paths both verified directly against production with no regression from `PJ-004`, no sensitive/internal detail leaked, and full account cleanup verified (`delete_my_account()` succeeded, data confirmed gone, re-login now fails). No finding status changed — this closed out the last piece of Phase A from `00_09` §16.

**OB-006 Sentry integration (2026-09-05):** the owner created a real Sentry project and provided its DSN directly (no wizard, no credential ever placed in the app). The existing integration required no code changes — inspection confirmed it was already correct, including `sentry_flutter`'s automatic release/build-version population. One controlled, temporary test exception was sent through the exact production wiring and received a real, server-confirmed event id from Sentry, with the fake secrets embedded in the test message correctly redacted before send and no duplicate-reporting path found.

**Correction (2026-09-05, same day):** the operator clarified that this event was observed running in the `development` environment (a local `flutter test`/Dart-VM run), not from a compiled release or staging build — and that the original `OB` remediation plan's own closure bar for this item (`OB_remediation_plan.md` R2-1) explicitly requires confirming the mechanism "in a test/staging environment," not development. **`OB-006` is therefore corrected to `PARTIALLY_REMEDIATED`**, not `VERIFIED_CLOSED` — the integration code, transport, redaction, and duplicate-reporting behavior are all proven; what remains is one release/staging build emitting a real event, which is item 8 of the Release Engineering wave below. My prior statement above ("this closes the last open finding... RR-001 and PJ-006 are the only findings from this entire wave still not VERIFIED_CLOSED") was inaccurate on two counts, corrected in full in the reconciliation below: (1) `OB-006` itself was prematurely marked closed, and (2) the claim never accounted for `SEC-001`/`ROOT-002` — a genuinely still-open finding from an earlier wave, detailed next.

---

## Full-Engagement Reconciliation (2026-09-05, before Release Engineering begins)

Performed at the operator's explicit request, after a self-identified overclaim (above). Method: every finding this engagement ever touched was re-checked against actual current code/doc state; every domain the operator named was swept for any remediation evidence at all, not assumed closed by omission.

**`SEC-001` / `ROOT-002` — still OPEN, not VERIFIED_CLOSED.** `00_09` §14 states explicitly: "Once rotation is complete... this session will re-run the authenticated runtime-test pattern... and update SEC-001/ROOT-002 based on that result — not before." **That re-verification never happened** — the narrative moved directly into the next remediation wave (§15) without confirming rotation completion or re-testing the deployed functions against a rotated key. Two of three closure conditions remain met (Edge Function deployment + client-artifact verification, both evidenced in `00_09` §Operational Closure Checkpoint); the third (key rotation, confirmed and re-verified) is **still outstanding** — not because rotation didn't happen, but because this session never checked and never asked. **Owner action / next step:** confirm whether the Google Cloud rotation was completed; if so, this session can re-run the authenticated 4-function smoke test in minutes and close both findings; if not, the old key should be treated as still live and this remains a real residual exposure.

**Fiqh/Search-grounding degradation — re-tested live this wave (AI Security wave, 2026-09-05), unchanged, still classification B (DEGRADED).** Live call against `fiqh-advisor-chat` on the isolated local stack reproduced the exact same failure: `Gemini request failed (429).` — the identical Google Search-grounding quota/billing condition first documented in the Operational Closure Checkpoint, confirmed via server-side function logs (not inferred from the fallback response alone). Fails safely with a clear message, no silent ungrounded fallback, no citations fabricated — blocked on a Google Cloud quota/billing condition on the Search-grounding tool, an owner action, not a code defect. Still accurately DEGRADED.

**`W0-002`** — confirmed unchanged: `DEFERRED — PRODUCT/DATA-MODEL DECISION REQUIRED`. No schema/data-model work has touched `pregnancy_tracking` at any point since.

**`W1-001`** — confirmed unchanged: `OPEN`, design-only (DB-backed rate limiter proposed, not implemented; explicit approval required before any implementation, not given).

**`RR-001`** — confirmed accurate: `PARTIALLY_REMEDIATED`, one remaining gap (pregnancy-tracking's unobserved sync failures, behind `W0-002`).

**`PJ-006`** — confirmed accurate: `OPEN` by design, a Doctor's Report product gap out of scope for persistence-layer remediation.

**`DC-004` — a real, previously-uncredited fix found; upgraded to `VERIFIED_CLOSED`.** The finding's exact described defect — "`runZonedGuarded` swallows `AppEnvironment.load()` startup failures via `debugPrint` only... silent infinite splash-screen hang, not a visible error" — **no longer exists in the current code**. `lib/main.dart`'s config-load `try/catch` sits deliberately *outside* `runZonedGuarded` (its own comment names `DC-004` directly), reports the failure via `AppErrorReporter`, and renders a real, visible `_StartupErrorApp` screen instead of hanging on the native splash. This was introduced during the Reliability+Observability wave (`00_09` §15/§16) as a byproduct of wiring `AppErrorReporter` into `main.dart`, but the finding register row was never updated to reflect it — corrected now, with the current code as evidence.

**Release/Deployment domain — 100% untouched, all findings exactly as originally audited.** `RD-006` (static build number), `RD-007` (no privacy policy/CHANGELOG), `RD-009` (no feature-flag/rollback) — all `BLOCKER`, `OPEN`. `RD-004` (SDK pinning), `RD-008` (git history), `RD-010` (keystore custody) — all lower-severity, `OPEN`. `RD-001`/`RD-002`/`RD-003`/`RD-005` (formal re-adoptions of `DC-005`/`SEC-003`, `DC-010`, `DC-006`, `DC-001`/`DC-003` respectively, per the note at the end of the BLOCKER table) track their `DC`/`SEC` counterparts below — none independently remediated. This is the domain about to be addressed by the Release Engineering wave beginning now.

**Dependencies/Config domain — mostly untouched.** `DC-001` `PARTIALLY` improved (secret exposure closed via the Gemini remediation; asset-bundling architecture itself unchanged) — `OPEN`. `DC-002` (dead `APP_ENV`), `DC-007` (no SDK pinning), `DC-008`/`DC-009`/`DC-011` — all unchanged, `OPEN`/unknown. `DC-003` — `VERIFIED_CLOSED` (unchanged from earlier). `DC-004` — newly upgraded to `VERIFIED_CLOSED` above. `DC-005` (Android debug signing) and `DC-010` (iOS unpinned team) — unchanged, `OPEN`, both squarely in scope for the Release Engineering wave beginning now. `DC-012` — already closed/false-positive at original audit time, unchanged.

**Accessibility/UX domain — 100% untouched.** `AU-001` (icon-only buttons, `BLOCKER`), `AU-002`, `AU-003` (WCAG contrast), `AU-004`, `AU-006`, `AU-007`, `AU-009` (`BLOCKER`, no controlled interaction testing ever executed) — all confirmed `OPEN`, unchanged, no code or process work has touched this domain. **Not independently re-confirmed this pass:** `AU-005`, `AU-008` — not itemized in this register's own table (referenced only via `ROOT-010`'s manifestation list); presumed unchanged/`OPEN` by the domain-wide pattern, but not individually re-verified against `AU_findings.md` in this reconciliation. Flagged honestly rather than asserted.

**Backup/Recovery domain — untouched except one Wave-0 closure.** `BR-001` (zero backups/PITR confirmed, `BLOCKER`), `BR-002` (migration replay fails, `BLOCKER`, independently reproduced), `BR-003`, `BR-004` (no DR runbook), `BR-005`, `BR-006`, `BR-008` (no restore ever demonstrated against the real system — my own Wave 0 restore test was against an isolated local reproduction, which does not satisfy this finding for the actual production system) — all confirmed `OPEN`, unchanged. `BR-007` — `CLOSED` in Wave 0 (Storage confirmed unused), unchanged since. **`BR-001`/`BR-002` are explicitly out of scope for any wave in this engagement** — both require production DB/schema-level intervention (a Supabase plan/PITR change and a migration-history repair, respectively), both are on every "not authorized" list the operator has given, including the current one.

**Privacy domain — 100% untouched.** `PC-001` (cosmetic consent, `BLOCKER`), `PC-002` (no deletion UI — the `delete_my_account()` RPC itself works and has been repeatedly verified functional during this engagement's own QA cleanups, but no end-user-facing UI ever calls it, so the finding's actual scope remains open), `PC-003` (undisclosed third-party AI processing), `PC-004` (no privacy policy document, duplicate of `RD-007`), `PC-005`, `PC-006`, `PC-007`, `PC-008` (no DPA/subprocessor terms — legal/contractual, not code-fixable), `PC-009` (no age-gate) — all confirmed `OPEN`, unchanged. None of this domain has been in scope for any remediation wave to date.

**Net correction:** far more than two findings remain open across this engagement. `RR-001` and `PJ-006` were accurately the only two *from the immediately-preceding Reliability+Observability wave* not fully closed — but that wave never touched Release/Deployment, most of Dependencies/Config, Accessibility/UX, most of Backup/Recovery, Privacy, or the still-unresolved `SEC-001`/`ROOT-002` rotation gate, all of which remain open blockers today. **Overall verdict remains 🔴 NO-GO** — seven of the ten root causes in this register's own Root-Cause Consolidation table are still rated Highest/High priority and unresolved (`ROOT-003` signing, `ROOT-004` CI/CD, `ROOT-007` backup/schema-recovery, `ROOT-008` rollback, `ROOT-009` consent/privacy, `ROOT-010` accessibility, and `ROOT-002` pending the rotation gate above).

---

## Release Engineering Wave (2026-09-05) — see `00_09` §19 for full evidence

Ten-item wave addressing `ROOT-003`/`ROOT-004` directly. Summary: Android core-library desugaring **VERIFIED_CLOSED** (real release build now succeeds — previously failed outright); `DC-005`/`SEC-003` (Android debug signing) **VERIFIED_CLOSED** with a real generated keystore, one owner custody action (back up or replace the keystore before real upload); `DC-010` (iOS team) remains **OPEN** — requires the owner's real Apple Developer Team ID, not obtainable this session; `RD-006` (build numbering) and `DC-006` (CI/CD) **PARTIALLY_REMEDIATED** (real process/pipeline created, not yet fully enforced/verified against live infrastructure); `DC-007` (SDK pinning) **VERIFIED_CLOSED**; `DC-002` (dead `APP_ENV`) **PARTIALLY_REMEDIATED** — and in the process of verifying this, **a real, previously-unknown bug was found and fixed**: every build type bundled the same static `.env`, so a release build had no way to ever report the correct environment to Sentry (fixed via a compile-time `--dart-define=APP_ENV` override). `RD-009` (no rollback) remains **OPEN**, now honestly documented instead of unaddressed. `OB-006` remains **PARTIALLY_REMEDIATED** — release/staging Sentry event confirmation was attempted extensively (four distinct on-device verification techniques, each blocked by a genuine Android platform constraint, not an application defect) but not achieved; reported honestly rather than claimed. Two real, working release artifacts were produced and inspected: `app-release.apk` (73.0MB) and `app-release.aab` (68.3MB), both correctly signed, both confirmed free of `GEMINI_API_KEY`/`service_role` leakage. `flutter test`: 276/284, identical failing set to every baseline this engagement — zero regressions.

---

## Backup / Recovery Remediation Wave (2026-09-05) — see `00_09` §20 for full evidence

**`BR-001` (managed backups/PITR):** carried forward from Wave 0 (`pitr_enabled: false`, zero backups) — **not independently re-confirmed live this wave**, the same recurring Supabase CLI keychain-auth hang blocked every authenticated command, stated plainly rather than glossed over. **`BR-002` (migration replay failure): re-confirmed live, freshly, this wave** — starting a truly empty local Supabase stack and letting it auto-replay `supabase/migrations/` failed outright, direct reproduction rather than inference. **A real, working, timed restore was demonstrated**: the canonical baseline (`supabase/canonical_baseline/00_public_baseline_draft.sql`, re-confirmed **CURRENT**) applies cleanly to empty in under 2 minutes total (92s to schema-ready, ~6.75 minutes including full behavioral validation), producing 24/24 tables, 8/8 functions, 24/24 RLS-enabled, verified this time against the real local Supabase Auth service rather than a hand-stubbed approximation. 12 of 14 required behavioral checks passed cleanly (auth signup, RLS enforcement both directions, cycle/pregnancy/profile/community writes, `delete_my_account()` extended to two more tables than Wave 0 checked). **One check (prayer tracking) surfaced a real, previously-unknown, currently-live production bug**: `W0-001`'s table-name fix is confirmed still in place, but a separate, newly-discovered defect (`W0-003`, new) means the app sends `PrayerStatus` enum values that don't match the database's CHECK constraint for 3 of 4 possible statuses — this corrects `00_10`'s prior "W0-001: fixed" claim, which overclaimed. **Schema/application recoverability and real production user-data recoverability are kept explicitly separate, per instruction**: the former is now demonstrated and current; the latter **does not exist** — no PITR, no independent logical data backup, no manual backup found anywhere. A full production data backup strategy design (cadence, retention, encryption, off-platform storage, access controls, restore-testing cadence, release-time triggers) was produced and is `OWNER_ACTION` — it requires plan-tier/billing/infrastructure access this session doesn't have, and the finding is not weakened by that unavailability. `BR-004` (no runbook) and `BR-008` (no restore ever demonstrated) move to `PARTIALLY_REMEDIATED`; `BR-007` (Storage unused) is corrected to `VERIFIED_CLOSED` in its own row (a closure Wave 0 actually already established but never propagated to the register row until now). A recovery runbook (`BR_recovery_runbook.md`, new) and a release-time recovery-readiness gate (added to `RD_release_rollback_runbook.md`) were both produced. `dart analyze`/`flutter test` not re-run — no Flutter/Dart code changed this wave; the prior baseline (276/284) is unaffected and current.

---

## W0-003 Remediation Wave (2026-09-05) — see `00_09` §21 for full evidence

**`W0-003` → VERIFIED_CLOSED.** An explicit persistence mapping between `PrayerStatus` and `prayer_log`'s real CHECK-constraint values was added at exactly the repository's remote boundary, leaving local storage untouched. Validated end-to-end (real repository code, live-schema-derived isolated environment): all three real statuses round-trip correctly, updates work with no duplicates, an unknown-but-valid database value (`qadha_required`) is reported rather than silently coerced, and the database itself correctly rejects a truly invalid value. **A second, independently-blocking bug was found and fixed in the same pass**: `scheduled_time`'s real column type (`timestamp with time zone`) didn't match what the app sent either — registered as `W0-004` (new), **VERIFIED_CLOSED**, including a real timezone bug (caught by its own test) corrected to a timezone-neutral encoding rather than the running machine's local timezone. **`W0-001` reassessed and upgraded to `VERIFIED_CLOSED`**: its own closure criteria required a live remote round-trip test that had never been run — it has now been run, and only succeeds because all three fixes (table name, status mapping, scheduled_time mapping) are in place together. `dart analyze`: 27 pre-existing, zero new. `flutter test`: 285/293, same 8 pre-existing golden-image diffs, zero regressions (9 new tests added, directly re-run and confirmed, not assumed from a stale count). No production DB changes; `W0-002`/`W1-001` untouched; every other finding preserved exactly as it stood.

---

## Privacy / Compliance Remediation Wave (2026-09-05) — see `00_09` §22 for full evidence

`PC-001` (decorative consent) and `PC-002` (no account deletion) **VERIFIED_CLOSED** — both real, exploitable-as-described defects at the start of this wave, both fixed and validated end-to-end (a real widget test proving consent genuinely blocks sign-up; a real isolated-stack test proving account deletion clears the session and blocks re-login). One real bug caught and fixed in the process: `delete_my_account()`'s RPC alone doesn't clear the client's local session — without the explicit local `signOut()` added this wave, the app would have kept behaving as authenticated after a successful deletion. `PC-003` (Gemini disclosure) and `PC-004`/`RD-007` (privacy policy) **PARTIALLY_REMEDIATED** — disclosure and in-app reachability both fixed; Gemini data minimization was audited and found *already* correct (no code change needed, confirmed by reading the actual server-side context-building code); public URL hosting for app-store submission is `OWNER_BLOCKED`. `PC-006` (data export) **PARTIALLY_REMEDIATED** — a real, RLS-verified JSON export now exists alongside the 4 PDF reports, explicitly scoped and stated as partial. `PC-005`/`PC-007`/`PC-008`/`PC-009` remain **OPEN**, unchanged, correctly out of this wave's scope (labeling mismatch, undefined retention duration, legal/contractual DPA review, age-gate) — `PC-007` gained one honest addendum (local device data also survives account deletion today, not previously documented this precisely). Local-storage encryption (Phase H) was assessed as a real, currently-true gap for the most sensitive categories (cycle/prayer data in plaintext `SharedPreferences`) but the migration itself was deliberately **not** attempted — a genuinely risky, cross-cutting change correctly left as a precise design, not silently performed. `dart analyze`: 27 pre-existing, zero new. `flutter test`: 289/297, same 8 pre-existing golden-image diffs, zero regressions (4 new widget tests, directly re-run). No production DB changes; every previously-open finding not touched by this wave preserved exactly as it stood.

---

## Local Sensitive Storage + Account Deletion Cleanup Wave (2026-09-05) — see `00_09` §23 for full evidence

Implements exactly what the Privacy/Compliance wave's Phase H deferred as a precise design: `flutter_secure_storage` (Android Keystore / iOS Keychain, platform-managed keys) now backs cycle, prayer, and pregnancy local data, migrated non-destructively from the prior plaintext `SharedPreferences` caches (7-step verified/idempotent/failure-safe migration, tested). **A real, severe, previously-undocumented defect was discovered and fixed in the same pass, registered as `PC-010` (new) — VERIFIED_CLOSED**: all three local caches used a single global, unscoped storage key, meaning a second person signing in on a shared device after the first signed out could read the first person's cached health data (cycle tracking had zero filtering at any layer). Fixed by resolving the current user id internally and scoping every storage key by it — structurally eliminates the leak without any explicit wipe-on-logout, validated by a mandatory dedicated test (User A writes data → simulated logout → User B signs in → reads back empty; User A's data unaffected — run for both cycle and prayer tracking). `PC-007`'s addendum (local data surviving account deletion) is now **VERIFIED_CLOSED** — `AuthRepositoryImpl.deleteAccount()` clears cycle/prayer/pregnancy local data after the RPC succeeds, with per-category failure isolation, `AppErrorReporter` evidence on partial failure, and startup retry; `PC-007`'s core finding (no retention *duration* defined anywhere) remains correctly `OPEN`, not invented. `BR-006` (on-device backup design) **PARTIALLY_REMEDIATED** — Android's `allowBackup="false"` already excludes everything; iOS's actual default Keychain accessibility was checked directly against the package source (not assumed) and hardened from `unlocked` to `unlocked_this_device` so an encrypted local backup restore onto a new device can't surface old ciphertext. **Two real regressions were caught and fixed by this wave's own testing before being reported as complete**: a Dart generic-inference bug (`const []` inside a generic method silently becomes `List<Never>`, not `List<T>`) that crashed the dashboard's prayer-status card on empty data, cascading into 13 widget-test failures; and a test-isolation gap where several `parity_*` test files calling the shared test harness multiple times per file leaked stale secure-storage state between sub-tests. Both fixed and the full suite re-run clean afterward. `dart analyze lib/`: 27 pre-existing, zero new. `dart analyze test/`: zero. `flutter test`: **300/308** — prior baseline (289/297) plus 11 new tests, same 8 pre-existing golden-image diffs byte-for-byte, zero regressions, directly re-run three times over this wave (twice catching the regressions above, once confirming the clean baseline). `prayer_location`'s raw GPS cache explicitly evaluated and deliberately deferred (real residual gap, out of this wave's named scope, not silently left worse). No production DB changes; `W0-002`/`W1-001` untouched; every other finding preserved exactly as it stood. **Overall verdict remains NO-GO** — this wave closed a severe newly-discovered privacy defect and a previously-deferred encryption gap, but does not touch the still-open blockers driving the NO-GO verdict (`SEC-001` key rotation, `BR-001`/`BR-002` backup/recovery, `RD-006` build numbering, `RD-009` rollback, the entire Accessibility domain, among others).

---

## Accessibility / UX Remediation Wave (2026-09-05) — see `00_09` §24 for full evidence

`AU-001` (15+ unlabeled icon-only controls) **VERIFIED_CLOSED** — every confirmed site now has a `tooltip:`/`Semantics(label:)`, including the one bare `GestureDetector` close button that had no accessible name or role at all; a new shared `NiswahIconButton` primitive exists for future controls. Validated via Flutter's real semantics tree (`tester.ensureSemantics()`), not just code reading, across 10 targeted tests covering signup, consent, password visibility, account deletion, cycle tracking, prayer tracking, Dr. Niswah, privacy policy, data export, and navigation. `AU-002`/`AU-004`/`AU-006`/`AU-008`/`AU-010` **VERIFIED_CLOSED** — phase-timeline semantic grouping, symptom-severity non-color indicator + touch-target fix, dashboard text-scale-safe `FittedBox` wrapping (tested at 1x-3x scale in both languages), the confirmed RTL padding bug plus one more found in the same sweep, and decorative-element exclusion, respectively. `AU-003` **VERIFIED_CLOSED** — a hue-preserving darkened text-safe color variant applied only at the two confirmed small-text usage sites, base brand colors left untouched elsewhere (visual identity preserved per the wave's own constraint); affected goldens re-captured. `AU-005`/`AU-007` **PARTIALLY_REMEDIATED** — the SnackBar's error/success states now carry a non-text visual signal (no `Form`/`validator` migration attempted since the form has no fields that are actually required to validate); a new automated regression test guards against the RTL-padding bug class recurring, while the ARB/`gen-l10n` migration and expanding golden coverage past 24% remain explicitly out of scope, per the specialist's own recommendation. `AU-009` **remains OPEN** — no live device/screen-reader/AT testing was performed (no such tooling available in this session, the same constraint stated in every prior wave); per the specialist audit's own decision rule, this alone still independently drives a NO-GO verdict for this domain regardless of how thoroughly the static/automated-semantics findings are addressed.

**Three real, previously-undocumented defect classes were discovered in the course of this wave's own testing, not by static reading — all found and fixed in the same pass:**
- **`AU-011` (new) — VERIFIED_CLOSED, severity HIGH.** Several custom `Semantics(label:)` wrappers lacked an explicit boundary (`excludeSemantics`/`container: true`), so Flutter silently merged them with sibling/descendant text. In the cycle-log sheet specifically, the close button's semantics merged with the header's title/subtitle into one node carrying both the informational text and the close action — a screen-reader user activating what reads as ordinary text would have actually triggered "close the sheet," a real mis-activation risk, not merely redundant audio. The same class (redundant, non-misleading duplication) was independently found and fixed in the symptom chips, the bottom nav bar, the dashboard notifications button, a wellbeing check-in close button, the community engagement bar, cycle-calendar day cells, and the cycle ring. Verified via live semantics-tree dumps before/after, not assumed.
- **`AU-012` (new) — VERIFIED_CLOSED, severity MEDIUM.** The sign-up consent checkbox — the single control gating account creation — is a hand-drawn `InkWell`/`AnimatedContainer` with no `Semantics` wrapper at all; a screen reader could not recognize it as a checkbox or announce its state. Fixed with an explicit, independently-activatable, state-announcing `Semantics` node.
- **`AU-013` (new) — VERIFIED_CLOSED, severity LOW.** A physical `Alignment.centerLeft` in `dr_niswah_chat_screen.dart` (same root cause as `AU-008`) — fixed to `AlignmentDirectional.centerStart`.

---

## AI Security + Abuse Control Remediation Wave (2026-09-05) — see `00_09` §25 for full evidence

**`W1-001`** (in-memory rate limiter proven ineffective) — the disproven limiter is replaced with a durable, Postgres-backed atomic counter (`ai_rate_limit_counters` table + `check_and_increment_ai_rate_limit()` `SECURITY DEFINER` RPC), fully implemented in a new migration and exhaustively validated against a disposable isolated local Supabase stack: all 13 required scenarios pass, plus a real 25-concurrent-request HTTP load test directly against the actual `ai-assistant-chat` Edge Function (**15 allowed, 10 rejected, 0 unexpected, 0 Gemini calls among the rejected** — clean numerical proof, the same kind of evidence that originally disproved the old limiter). The `dr-niswah-chat` red-flag exemption (`AB-008`) was directly re-confirmed to still bypass the limiter correctly even at 20/15 over quota. Fail-closed behavior (limiter backing store unavailable → HTTP 503, no Gemini call, no DB internals leaked) was directly tested by revoking the RPC's execute grant mid-session. **`W1-001`/`AB-002`/`SEC-005`/`AB-008` move to `PARTIALLY_REMEDIATED — CODE_COMPLETE / LOCALLY_VERIFIED`** — explicitly **not** `VERIFIED_CLOSED`, since the migration has not been applied to the production database, per this wave's explicit production-safety boundary. A full production deployment package (migration hash, preconditions, exact commands, smoke-test/load-test sequence, rollback strategy, closure criteria) is prepared in `00_09` §25 but **not executed**.

**`SEC-001`/`ROOT-002`** (exposed Gemini key, rotation pending) — re-confirmed via a broader scan this wave (compiled Dart AOT binary directly searched for the key pattern, not just the `.env` asset; full git history re-searched — zero committed Gemini key material found; the one unrelated `AIza…` match in history is a pre-existing, already-tracked Firebase Web API key in the orphaned `firebase-applet-config.json`, not a Gemini credential). **New evidence**: a read-only `supabase secrets list` (returns a hash digest, never the plaintext value) shows the deployed `GEMINI_API_KEY`'s `updated_at` timestamp predates the Edge Function migration itself — confirming this is still the original, never-rotated key. Rotation was attempted this wave and is confirmed genuinely `CREDENTIAL_ROTATION_OWNER_BLOCKED`: no `gcloud` CLI or other authenticated Google Cloud/Gemini credential-management tooling exists in this session (checked directly, not assumed). **Remains `OPEN`**, unchanged in substance — rotation is still the sole gate to closure, now with stronger evidence that it hasn't happened and confirmation that this session cannot perform it.

**Fiqh Search-grounding** — re-tested live against the isolated stack this wave: identical `Gemini request failed (429).` from Google's Search-grounding quota/billing limit, confirmed via server-side function logs. **Still classification B (DEGRADED)**, unchanged — fails safely, no silent ungrounded fallback, blocked on an owner-side Google Cloud quota/billing condition.

No production DB schema/migration/RLS/trigger/function/RPC was applied to production this wave — the new migration exists only in the repository and was tested exclusively against a disposable local stack, matching this wave's explicit production-safety boundary. `W0-002` untouched. `dart analyze`/`flutter test` not re-run — no Flutter/Dart application code changed this wave (confirmed via `git status`; only `supabase/functions/` and `supabase/migrations/` files touched), so the last-known baseline (`319/327`, same 8 pre-existing golden-image diffs) is unaffected and remains current. Every other finding not touched by this wave preserved exactly as it stood: `W0-002` (`DEFERRED`), `RR-001` (`PARTIALLY_REMEDIATED`), `PJ-006` (`OPEN`), `BR-001` (`OPEN`), `RD-009` (`OPEN`), `DC-010` (`OPEN`), `OB-006` (`PARTIALLY_REMEDIATED`), remaining Privacy/Compliance findings, `AU-009` (`OPEN`), and the entire Accessibility domain otherwise unchanged. **Overall verdict remains NO-GO** — this wave closed the *design and validation* gap for AI abuse control, but production deployment of the fix, Gemini key rotation, and every other still-open blocker (`BR-001`/`BR-002`, `RD-006`, `RD-009`, `AU-009`, among others) remain outstanding.

---

## W0-002 Pregnancy Tracking Data-Model Remediation Wave (2026-09-06) — see `00_09` §26 for full evidence

**Deeper root cause found this wave, not previously documented anywhere in this engagement**: `PregnancyTrackingScreen` — the only consumer of the broken `pregnancy_milestones` table reference — is unreachable from any navigation route in the entire app (confirmed via exhaustive `grep` and via its own git history: present, unwired, since the repository's first commit). Separately, `pregnancy_records` — the live table `W0-002` originally assumed was the intended-but-incompatible backing store — is **not queried by any current Flutter code at all**; it was quietly superseded by `pregnancy_profile` (a later, correctly-designed, actively-used table) and left live but orphaned. Both facts were verified directly from code, not inferred.

**`W0-002` resolved at the code/design/local-validation level**: a new `pregnancy_milestones` table — a direct per-user child table (FK to `users(id) ON DELETE CASCADE`), matching this schema's existing `wellbeing_logs` convention rather than inventing a parent/child relationship to the orphaned `pregnancy_records` (the app's own `PregnancyMilestone` entity has never had a pregnancy-parent-id field, and `pregnancy_profile`'s own `UNIQUE(user_id)` confirms the app has no concept of multiple concurrent/historical pregnancies that would justify one). `pregnancy_records` itself is left completely untouched by this migration — its actual disposition (real historical data vs. genuinely vestigial) is an explicit, separate, deferred question this session cannot safely answer without live production read access. `PregnancyTrackingRepositoryImpl` was rewritten to the real table with proper `AppErrorReporter` reporting (previously silently swallowed every remote failure via bare `catch (_) {}`) and a local-authoritative-with-sync `PregnancySyncStatus`, matching `CycleLog`'s established `RR-001` pattern exactly. The ViewModel's hardcoded fake pregnancy data (`DateTime.now().subtract(30 weeks)`, used regardless of any real user input) was replaced with the real `PregnancyStatusController` state the dashboard's own pregnancy overview already uses.

**Fully validated against a disposable isolated local Supabase stack**: 14/14 RLS/CRUD tests (own-row create/read/update/delete, cross-user read/update/delete denial via RLS, unauthenticated denial, spoofed-`user_id` insert denial via `WITH CHECK`, malformed-input rejection via `CHECK` constraints), a direct account-deletion cascade test (a milestone row created for a test user was confirmed gone, with zero orphans, immediately after calling the real `delete_my_account()` RPC), and a combined fresh-restore test confirming this migration applies cleanly on top of the canonical baseline **together with** the AI Security wave's still-pending migration — no conflict between the two waves' schema changes. `PC-006` (data export) extended to include `pregnancy_milestones`, and a stale doc-comment incorrectly still claiming prayer data was also excluded (already fixed in the `W0-003` wave, comment never updated) was corrected in the same pass. `RR-001` extended to pregnancy tracking (same sync-status/classification mechanism as cycle tracking), deliberately not wired to an app-wide lifecycle retry trigger since the feature isn't reachable from navigation to begin with. `PJ-006` re-traced and confirmed genuinely unaffected — Doctor's Report has only ever aggregated from `pregnancy_profile`, never `pregnancy_milestones`, and still doesn't; correctly left `OPEN`, not closed.

**Not `VERIFIED_CLOSED`**: per explicit instruction, the migration has not been applied to production. `dart analyze lib/`: 27 pre-existing, zero new. `flutter test`: **323/331** — prior baseline (319/327) plus 4 new tests (`pregnancy_tracking_test.dart` grew from 3 to 7, adding `PregnancySyncStatus` coverage and two tests proving the ViewModel now reflects real `PregnancyStatusController` state instead of a fabricated constant), same 8 pre-existing golden-image diffs byte-for-byte, zero regressions — directly re-run and confirmed. No production DB changes; `W1-001`'s pending migration, `SEC-001`/`ROOT-002` (rotation still owner-blocked), Fiqh Search-grounding (`B — DEGRADED`), the entire Accessibility domain, and every other finding not touched by this wave preserved exactly as it stood. **Overall verdict remains NO-GO** — this wave closed a real, long-deferred schema/code defect and surfaced a deeper, previously-undocumented root cause (an entire feature unreachable from navigation), but production deployment of this fix, Gemini key rotation, `W1-001`'s own production deployment, and every other standing blocker remain outstanding.

**Two real regressions were caught and fixed by this wave's own testing before being reported as complete** (a `flutter test` sweep specifically, not assumed clean): a debug-probe investigation into why several accessibility tests failed traced back to genuine app-behavior gaps (e.g. `ProfileScreen`'s delete-account row not yet built off-screen, `DataExportScreen` correctly requiring auth) rather than test-authoring mistakes in every case — each was verified against the real rendered widget tree via live probes before the test assertions were corrected, not guessed at. `dart analyze lib/`: 27 pre-existing, zero new. `dart analyze test/`: 6 new info-level deprecation notices (`hasFlag`, still functional, not migrated to the newer `flagsCollection` API — a polish item, not an error). `flutter test`: **319/327** — prior baseline (300/308) plus 19 new tests (10 semantics, 8 text-scaling, 1 RTL-lint regression), same 8 pre-existing golden-image diffs byte-for-byte after the AU-003 color fix's 2 affected goldens were re-captured, zero unexpected regressions — directly re-run three times over this wave (once surfacing the 2 expected insights-golden diffs from the intentional contrast fix, once confirming the re-capture, once as final verification). No production DB changes; `W0-002`/`W1-001`/`SEC-001`/`RR-001`/`PJ-006`/`BR-001`/`RD-009`/`DC-010`/`OB-006`/remaining Privacy findings/`prayer_location` all preserved exactly as they stood. **Overall verdict remains NO-GO** — `AU-009` (no live AT/device testing performed) alone still independently triggers it per the specialist audit's own rule, and the engagement's other open blockers (`SEC-001` key rotation, `BR-001`/`BR-002`, `RD-006`, `RD-009`, among others) are untouched by this wave.

---

## Pregnancy Tracking Product Integration + RR-001 Closure Wave (2026-09-06) — see `00_09` §27 for full evidence

**No production DB schema/migration/RLS/data change of any kind was made this wave**, and no `W0-002`/`W1-001` production deployment, Gemini rotation, iOS signing, rollback kill-switch, or `AU-009` work was performed, matching this wave's explicit stop condition exactly.

**`PregnancyTrackingScreen` was classified as Classification C — duplicated/superseded by another current feature**, not an unwired bug — using product evidence, not file existence alone: the dashboard's own `_PregnancyOverview` widget already provides a materially richer pregnancy status experience from the same underlying state; the design reference (`src/components/PregnancyTracker.tsx`) is embedded inside `Today.tsx`, never a standalone page, so this screen's standalone-route shape was a divergence, not a target; `PrayerTrackingScreen` shows an identical unreachable/superseded pattern, confirming an app-wide consolidation architecture rather than a pregnancy-specific accident; and the `planPregnancyMilestone` notification feature was investigated and correctly ruled out as an unrelated red herring. Per the explicit instruction not to force an abandoned feature into the product, **no navigation route was added.**

**A real bug was found and fixed**: `DataExportScreen`'s user-facing disclaimer (English and Arabic) incorrectly told users pregnancy-tracking data was excluded from their export, even though the underlying fetch already included it (added in the `W0-002` wave) — the same category of stale-disclaimer bug previously found and fixed for prayer data in the `W0-003` wave.

**An attempted deletion of the three now-classified-dead files (`pregnancy_tracking_screen.dart`, `pregnancy_tracking_view_model.dart`, `pregnancy_calculator.dart`) was blocked by the Claude Code auto-mode permission classifier** as a destructive action outside this session's unilateral authority. No workaround was attempted; the files were instead left in place, unwired, with detailed header comments documenting the full evidence trail, per the charter's own explicitly-offered non-destructive alternative.

**`RR-001` reassessed and correctly kept `OPEN`**: pregnancy tracking's sync/retry mechanism (`syncPendingMilestones()`) is code-complete and unit-tested, but with the consuming screen confirmed dormant, there is no live path through which a pending write could ever be created — this is documented as dormant-but-correct infrastructure, not claimed as a second reachable success alongside cycle tracking. `RR-001` is explicitly **not** closed on this basis: `RR-003`'s `PregnancyProfileRepository` silent-failure gap remains genuinely unaddressed, and per the charter's explicit instruction, that alone keeps this finding open. **`PJ-006` re-confirmed `OPEN` and unaffected** — Doctor's Report still never reads `pregnancy_milestones`; this wave's classification decision does not change report completeness in any way, so no redesign was performed.

**Testing**: `test/pregnancy_tracking_test.dart` extended from 7 to 10 tests — a create/edit/delete-history test and two multi-user local-isolation tests (reusing the `SecureLocalStore.debugUserIdOverride` test seam), all passing. `dart analyze lib/features/pregnancy_tracking/`: clean. `dart analyze lib/`: confirmed still 27 pre-existing issues, zero new. Full-suite `flutter test` re-run: **326/334** — prior baseline (323/331) plus 3 net new tests, same 8 pre-existing golden-image diffs byte-for-byte (`parity_community_test.dart` ×2, `parity_today_lower_test.dart` ×1, `parity_profile_test.dart` ×2, `parity_dashboard_test.dart` ×2, `parity_cycle_log_sheet_test.dart` ×1), zero regressions, directly confirmed via a full background run, not assumed. No production DB changes; `W1-001`, `SEC-001`/`ROOT-002`, Fiqh Search-grounding, `BR-001`, `RD-009`, `DC-010`, `OB-006`, remaining Privacy findings, the entire Accessibility domain, and every other finding not touched by this wave preserved exactly as it stood. **Overall verdict remains NO-GO** — this wave resolved genuine product-integration ambiguity with real evidence, fixed one real privacy-disclaimer bug, and reassessed `RR-001`/`PJ-006` correctly without overclaiming closure, but `RR-001` cannot close while `RR-003` remains open, and every other standing engagement blocker (Gemini key rotation foremost) is untouched by this wave's scope.
